# --- ROS 2 / colcon build artifacts ---
**/build/
**/install/
**/log/
**/__pycache__/
*.pyc
*.pyo

# --- large generated maps / SLAM databases (regenerated by running the stack) ---
*.db
*.npz

# --- vendored third-party clone (we use go2_exploration's frontier) ---
go2-sim/go2_ws/src/m-explore-ros2/

# --- cloned reference material + stray large copies (not our code) ---
wendy-research/
go2-sim/backups/
simulation/
go2-autonomous-inspection-sim/
go2-rtabmap-mapping-stack/
go2-inspection/
go2-ros2-inspection-main/
go2-sim/.convergence-backup/

# --- stray scratch notes + tool droppings (kept locally, not part of the deliverable) ---
*.mgc
1 weights REQUIRED for.md
https github com Zhefan.txt

# --- media / model weights / scratch ---
*.mp4
*.mov
*.pt
Untitled
TEAM-BRIEF.txt
claude_architecture_brief.md
wendy-go2-template-architecture.md
CLAUDE.md

# --- office temp + local backups ---
.~lock.*#
*-ORIGINAL.pptx
.DS_Store

# --- generated debug artifacts (tf2 view_frames, etc.) ---
frames_*.gv
frames_*.pdf

# stray root-level duplicate clone (the real one is under apps/)
/go2-ros2-bridge-only/
