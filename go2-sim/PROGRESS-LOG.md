# Progress log — Go2 autonomy simulation

Running history of milestones, checkpoints, and decisions. Newest at top.
(Design rationale lives in `docs/01-DESIGN-DECISIONS.md`; plan in `docs/02-ROADMAP.md`.)

---

## CHECKPOINT 53 — Mission state machine + structured event stream (M7b) — 2026-06-30
**Status:** 🟢 Added an explicit mission FSM + a structured JSONL event stream over the (previously
print-only) async orchestration. Wired defensively so observability can never break control flow.
Verified live. 21 unit tests pass.

- **`go2_inspection/mission_fsm.py` (pure / ROS-free, CI-tested):** a strict state machine
  `IDLE → PLANNING → NAVIGATING → INSPECTING → (READING) → [next zone] … → ROLLUP → DONE` (`ABORTED` from
  any active phase; illegal transitions raise). Each transition appends an event
  `{seq, t, state, kind, zone?, data?}` to an in-memory log **and** an append-only JSONL stream; a
  `read_events()` helper reads it back for `get_status`/dashboards. **+4 unit tests** (lifecycle, illegal
  transitions rejected, abort-from-active, JSONL round-trip).
- **Wired into `inspection_mission.py` — additive + bulletproof:** a `_ev(fsm, …)` helper swallows every
  FSM/IO error so the *event stream is observability only and can never raise into the mission*. The
  mission now emits `PLANNING(zones)` → per zone `NAVIGATING(target)` → `ARRIVED`/`NAV_FAILED` →
  `INSPECTING` → `INSPECT_DONE(n_objects)` → `READING`/`READ_DONE` → `ROLLUP` → `DONE(total)`, to
  `~/gauges/mission_events.jsonl`.
- **Verified live (maze, navigate-only):** the stream captured `PLANNING[zone_0] → NAVIGATING(2.93,1.96)
  → NAV_FAILED → ROLLUP → DONE(navigated:0)` — honest even when Nav2 was down, and control flow was
  unaffected. (Nav2 had dropped from an earlier-launched stack; the FSM recorded it rather than hiding it.)
- **Next (separate, careful step):** surface the live phase via `mission_control` `get_status` /
  a `get_events` service so MCP + a dashboard can poll it (defer — touches the working 13-service server).

---

## CHECKPOINT 52 — Ground-truth benchmarking (M7b) + M6 deferral (safety) — 2026-06-30
**Status:** 🟢 Added a CI-gated detection benchmark vs world ground truth (neither version had one).
Deliberately **deferred** the legacy deletions after mapping the launch graph — they're entangled with
live nodes.

- **Benchmark harness (`go2_inspection/benchmark.py`, `ros2 run go2_inspection benchmark`):** parses
  ground-truth object world-positions straight from the Gazebo world SDF, loads the per-zone
  `objects.json`, and computes **precision / recall / F1**, **mean localization error** (greedy per-class
  nearest-neighbour within a match radius), per-class recall, and writes `benchmark.{md,json}`. Pure /
  ROS-free → **+6 unit tests, suite now 17 pass**. On the maze run it correctly finds the **4 ground-truth
  gauges** and honestly scores **recall 0/4** (detection was OFF). When YOLOE/CLIP is available the same
  harness reports real P/R + localization error vs e.g. gauge_01 @ (5.85, 2.0). This is the rigor neither
  `-main` nor our wall-follower had.
- **M6 (legacy retirement) DEFERRED after a full launch-graph trace — NOT a blanket delete.** The include
  graph shows the legacy launches are entangled with the **live** path:
  `inspection_nav → nav2, rtabmap_slam → go2_champ, octomap` (so **octomap is live**), and the frontier
  launch `explore → nav2, slam → go2_champ, sim` (so **slam + sim are live** via exploration). Only
  `mission.launch.py` + `sim_mapping.launch.py` + the 4 wall-follower nodes
  (`zone_sweeper`/`zone_wall_follower`/`panorama_segmenter`/`yoloe_segmenter`) +
  `explore_lite`/`rtabmap.launch` sit outside the closure — and `sim_mapping → mission → those nodes` are
  interlinked. Per the standing *don't-break-working* rule, retiring them needs a careful, verified pass
  (build + launch-parse each active launch after), not a quick `rm`. Tracked for a dedicated M6 session.

---

## CHECKPOINT 51 — Full converged-inspection runtime test PASSES + a real hang fix — 2026-06-30
**Status:** 🟢 The converged `zone_inspector` engine runs **end-to-end in Gazebo**. Found + fixed a
detector-load hang that would otherwise stall every real inspection. 11/11 unit tests pass.

- **Live runtime verification (maze world, sim, Nav2 + localization up):**
  - **Async orchestration** — `inspect_zone` returns a `task_id` immediately; `get_status` reports
    `busy=inspect_zone:zone_0 task=running`; **`cancel_task` works** (`busy=idle task=cancelled`, robot
    stops). ✅
  - **Autonomous drive** — mission drove the robot **HOME → zone_0 via Nav2 → ARRIVED**, then launched
    `zone_inspector`. ✅
  - **The converged engine (after the fix below) ran the full FSM:** `2 viewpoints [(5.3,2.3),(2.3,2.3)]`
    → `NAV → viewpoint 1` → **`SPIN 360deg` (full turn 382°)** → `NAV → viewpoint 2` → **`SPIN 360deg`
    (383°)** → `inspection DONE → ~/gauges/zone_0` → **clean exit** (run-once contract). Wrote
    `objects.json` + `detections.json` + 4 viewpoint frame sets. ✅
  - **RGBD depth** confirmed earlier (RGB `/camera/image_raw` + real depth `/camera/depth/image_raw`
    0.49–5.62 m). ✅
  - **Only YOLOE detection itself is unexercised** — it needs the CLIP/MobileCLIP text backend (network),
    which is unavailable in this environment. Its absence is now handled gracefully (below).

- **🐛 Bug found + fixed — detector load could hang the whole node forever.** `zone_inspector.__init__`
  calls `detect_utils.load_model()` **synchronously**; with weights present but **no PE cache**, that
  reaches `model.get_text_pe()`, which **downloads the CLIP text backend with no timeout**. Offline it
  **blocks indefinitely** — so the node never starts its FSM and the robot **sits idle after arriving at
  the zone** (observed live: 0 `/cmd_vel`, `wchan=do_wait`, no logs, task stuck `running`). The intended
  graceful degradation never fired because the call **hangs** instead of **raising**.
  - **Fix** (`detect_utils.py`): wrap `get_text_pe` in `_get_text_pe_bounded()` — runs it on a daemon
    thread bounded by **`YOLOE_PE_TIMEOUT` (default 90 s)**; on timeout it **raises**, so `_load_detector`
    catches it → `model=None` → the node **navigates + spins normally, just captures nothing**. Proper cure
    remains the on-disk **PE cache** (build once with network); when it exists this path is never taken.
  - **Regression tests** (`test_detect_utils.py`, +3): bounded loader **times out → `TimeoutError`**,
    fast model **passes through**, erroring model **propagates**. Suite now **11 passed**.

- **Net:** the converged stack is verified working in sim **except** the (network-gated) YOLOE weights/CLIP
  step; the engine no longer hangs when that step is unavailable. **M6 (legacy retirement) is now
  unblocked** — the new inspect path is confirmed live.

---

## CHECKPOINT 50 — Converge onto the `-main` inspection engine (ADR-016) · M1 — 2026-06-30
**Status:** 🟢 M1 done (build clean, nothing running changed). Decision + full plan in `docs/05-CONVERGENCE.md`.

- **Discovery:** a teammate's `go2-ros2-inspection-main` is a **more advanced, cleaner parallel line** of
  this project. A 3-way review (our 2 versions + 2 independent subagents) found theirs ahead on most axes:
  **depth→map 3D object localization** (RGBD cam), **viewpoint + 360° spin** inspection, **async task model
  + `cancel_task`**, **position-validation + persistence dedup**, **modular** code (`detect_utils`/
  `report_utils`/`yoloe_tuner`), clean launch set, per-package docs. ONLY ours has **gauge value-reading**
  (`gauge_inspector`, Claude) + the **WendyOS apps**. NEITHER has tests/CI, a mission state machine,
  twist_mux/collision safety, benchmarking, or perception-sim realism.
- **Decision (owner-authorised full reuse):** converge onto their engine as the base, re-add our
  gauge reading + WendyOS apps, then exceed both. Staged M1–M7 (see ADR-016); build/verify each stage;
  git + a tarball backup (`.convergence-backup/`) are the safety net; sim first, WendyOS last.
- **M1 (this CP):** copied `zone_inspector.py` (920) + `detect_utils.py` + `report_utils.py` +
  `yoloe_tuner.py` into our `go2_inspection`; registered the `zone_inspector` console_script. **Purely
  additive** — wall-follower / mission_control / bringup / SLAM / Nav all untouched. `colcon build`
  clean; 15-class hazard vocabulary (imgsz 1280) + helpers import; new engine present but not yet wired.
- **Next:** M2 sim bringup (RGBD camera + direct `odom_tf` + depth bridge + `inspection_arena.sdf`) →
  M3 async orchestration + wire `inspect_zone→zone_inspector` → M4 re-layer gauge reading → M5
  door-aware segmenter + better frontier → M6 retire legacy → M7 tests/CI + state machine + safety +
  benchmarking. See `docs/05-CONVERGENCE.md`.

---

## CHECKPOINT 13 — Accurate odometry (gz OdometryPublisher) fixes localization drift — 2026-06-25
**Status:** 🟢 localization now ~5 cm accurate (was drifting ~0.2–1.5 m). Root-cause fix for the
wall-hitting/climbing + phantom 3D walls.

- **Diagnosis (via Gazebo GUI + TF vs gz truth):** the robot drove its whole body into walls and
  *climbed* them. Cause = **localization drift**: the CHAMP leg-odometry (`odom/raw`) drifts in the
  feature-poor corridor (gz truth (-0.77,1.44) vs costmap belief (-0.88,1.22) = ~0.22 m off), so the
  costmap places walls wrong → robot drives into the real wall → stiff position control climbs it →
  recovery "spin" fails ("Collision Ahead"). Same drift smeared the octomap (phantom walls).
- **Fix: accurate base odometry from `gz-sim-odometry-publisher-system`** (mirrors the real Go2's
  onboard EKF, which is accurate — NOT a cheat: SLAM/Nav2/frontier/obstacle-avoidance all still
  real). Added the plugin to `gazebo_gz.xacro` (publishes gz `/odom_gz`, gz.msgs.Odometry); bridged
  gz `/odom_gz`→ROS `/odom_gz` (note: plugin publishes on **global** gz `/odom_gz`, not
  `/model/go2/odom_gz`); `footprint_to_odom_ekf` `odom0: odom/raw → /odom_gz`. Verified: stopped,
  gz truth = map→base_link = odom→base_link within ~5 cm.
- **Also this session:** doorways 1.6→2.0 m; inflation 0.35→0.25; removed `raytrace_min_range` (it
  trapped the robot in a non-clearing ring); added `static_layer` to the LOCAL costmap (so DWB sees
  mapped walls inside the LiDAR 0.40 m dead zone).

**Next:** clean full run with accurate localization — confirm no wall-hits, clean room entry, clean
octomap. Then warehouse.

---

## CHECKPOINT 12 — 3D OctoMap + crash fix (slam_toolbox nav + octomap_server) — 2026-06-25
**Status:** ✅ 3D colored-voxel map building + robot navigates reliably (no crash/flip).

- **Crash diagnosis (robot drove WHOLE body onto walls):** introduced when I swapped slam_toolbox →
  **RTAB-Map** for SLAM. RTAB-Map's ICP localization on the LiDAR **drifts/jumps** → costmap walls
  misplaced vs reality → robot drives into real walls. slam_toolbox (CHECKPOINT 10, user-verified
  good avoidance) does not. **FIX: reverted navigation/localization to slam_toolbox.** Confirmed:
  robot upright (z=0.25, roll/pitch≈0), explored N→S, 9 goals, **no crash/flip**.
- **3D map (was flat with RTAB-Map Grid/3D=false → projected to xy):** switched to a **dedicated 3D
  mapper, `octomap_server`** (`go2_bringup/launch/octomap.launch.py`) running ALONGSIDE slam_toolbox.
  It consumes `/utlidar/cloud_deskewed` + the existing TF (map→…→utlidar_lidar) and builds a true 3D
  occupancy octomap — **walls included** (no flattening). Publishes `/occupied_cells_vis_array`
  (colored voxels by height, the pic-2 look) @10 Hz + `/octomap_full`. It does NOT localize
  (slam_toolbox owns map→odom) → nav stays reliable. Needs `sudo apt install ros-jazzy-octomap-server`.
  RViz `explore.rviz` now shows the octomap MarkerArray. Densified LiDAR to 16 vertical rings for a
  richer map. (RTAB-Map `rtabmap.launch.py` retained for reference but NOT used in the demo.)
- **Architecture (clean separation):** slam_toolbox = localization + 2D nav map; octomap_server =
  3D map viz; nav2 + frontier = autonomy; all sim-agnostic (same topics on the real Go2).

**RUN 3D DEMO (incremental, headless gz = machine-safe):**
```
ros2 launch go2_bringup slam.launch.py with_sim:=true headless:=true world:=facility.sdf
ros2 launch go2_bringup nav2.launch.py use_sim_time:=true     # wait "Managed nodes are active"
ros2 launch go2_bringup octomap.launch.py use_sim_time:=true
ros2 run go2_exploration frontier_explorer --ros-args -p use_sim_time:=true -p autostart:=true -p robot_base_frame:=base_link
ros2 run rviz2 rviz2 -d install/go2_bringup/share/go2_bringup/rviz/explore.rviz --ros-args -p use_sim_time:=true
```
**Next:** swap to the realistic warehouse world (nav2_minimal_tb4_sim/warehouse.sdf, Fuel meshes).

---

## CHECKPOINT 11 — Expert 3D map (RTAB-Map) + leg-clip/tipping fixes — 2026-06-25
**Status:** 🟢 RTAB-Map 3D LiDAR SLAM working; Nav2 footprint + gait/controller stability fixes in.

- **3D map (ADR-004 realized): RTAB-Map** (`go2_bringup/launch/rtabmap.launch.py`). Lidar-only mode
  (`subscribe_scan_cloud`) on `/utlidar/cloud_deskewed` + external CHAMP odom (`/odom`, Reg/Force3DoF).
  Provides `map→odom` (replaces slam_toolbox), `/map` (2D grid → Nav2/frontier, via `grid_map`→`/map`
  remap) AND **`/cloud_map` = assembled 3D point-cloud map** (the expert deliverable). VERIFIED:
  pose graph grows (WM=52+ nodes), `/map` @1 Hz, `/cloud_map` @0.5 Hz, `map→odom` published. Note:
  rtabmap publishes maps **lazily** (only when subscribed). "Missing visual features" warning is
  benign (lidar-only → ICP loop closure, not visual). Still runs p2l for Nav2's local /scan obstacle
  layer. Sim-agnostic: same `/utlidar/cloud_deskewed`+`/odom` on the real Go2. RViz `explore.rviz`
  now shows `/cloud_map` (PointCloud2, AxisColor Z).
- **Leg clipping wall → bigger costmap footprint:** trunk-only `0.40×0.24` → `0.60×0.36` +
  `footprint_padding 0.03` (covers leg/foot swing) so feet clear walls. (Verified by user.)
- **Tipping on tight turns → softer dynamics:** DWB `max_vel_theta` 0.8→0.5, `acc_lim_theta` 2.0→1.0,
  `acc_lim_x` 1.0→0.6, `max_vel_x` 0.30→0.26; gait `max_angular_velocity_z` 0.5→0.4,
  `swing_height` 0.04→0.05, `stance_duration` 0.25→0.28 (slower/taller step = more stable).

**Next:** full GUI demo (sim+rtabmap+nav2+frontier+rviz) in facility to watch the 3D map build +
confirm no tip; then swap to the realistic **warehouse** world (nav2_minimal_tb4_sim/warehouse.sdf,
Fuel shelves/pallets — needs internet on first run; heavier, watch machine).

---

## CHECKPOINT 10 — AUTONOMOUS MULTI-ROOM EXPLORATION WORKING + live viz — 2026-06-25
**Status:** ✅✅ **The Go2 autonomously explores the multi-room facility, building the map as it
goes.** Confirmed headless AND live in Gazebo GUI + RViz.

- **Result:** from origin the dog drove corridor → through doorways → into rooms; `/map` grew
  **1328 → 34,917 free cells** (13.3×16 m, multiple rooms, 2019 wall cells), **19 distinct frontier
  goals**. No stall — continuous coverage.
- **Final fixes that made it continuous:** (a) blacklist EVERY attempted goal (Nav2 SUCCEEDS within
  goal-tolerance → was re-picking the same frontier forever); `min_goal_distance` 0.5→0.8. (b) the
  360° LiDAR was seeing the dog's **own body** → phantom obstacle ring trapped the planner
  ("Collision Ahead"); fixed with LiDAR `range<min>` 0.40 + p2l `range_min` 0.45. (c) launch Nav2
  **promptly** after sim+slam (a long-running loaded sim caused a `/clock` lag → global_costmap
  "extrapolation into the past" → activation abort).
- **Viz:** `go2_bringup/rviz/explore.rviz` (map, RobotModel, /scan, frontiers, /plan). Gazebo GUI +
  RViz + full stack runs on the machine (~5.3 Gi free, load ~5).

**HOW TO RUN THE DEMO (incremental — the all-in-one overloads the machine):**
```
cd go2-sim/go2_ws && source /opt/ros/jazzy/setup.bash && source install/setup.bash && export DISPLAY=:1
ros2 launch go2_bringup slam.launch.py with_sim:=true headless:=false world:=facility.sdf   # gz GUI + sim + slam
ros2 run rviz2 rviz2 -d install/go2_bringup/share/go2_bringup/rviz/explore.rviz --ros-args -p use_sim_time:=true
ros2 launch go2_bringup nav2.launch.py use_sim_time:=true        # wait ~25s -> "Managed nodes are active"
ros2 run go2_exploration frontier_explorer --ros-args -p use_sim_time:=true -p autostart:=true -p robot_base_frame:=base_link
```
**Cleanup left:** remove the debug `RCLCPP_INFO` lines in frontier_explorer.cpp (findFrontiers/tick);
exploration slows once nearby north frontiers are blacklisted (greedy-nearest tail) — optional
upgrade to info-gain/largest-cluster selection; tune for full-facility coverage.

---

## CHECKPOINT 9 — Full autonomy stack integrated: Nav2 + frontier exploration — 2026-06-25
**Status:** 🟢 end-to-end stack runs; robot **autonomously navigates to frontiers and grows the
map**. Remaining: exploration stalls after a small area in the tiny `lab.sdf` world.

- **Nav2 (CONFIRMED active):** the stock `nav2_bringup/navigation_launch.py` aborts on Jazzy
  because it force-starts `docking_server` ("Charging dock plugins not given") + `waypoint_follower`
  + `collision_monitor` ("observation_sources not initialized"). Replaced with a **lean
  `nav2.launch.py`** that brings up only controller_server, smoother_server, planner_server,
  behavior_server, bt_navigator + a lifecycle_manager → "Managed nodes are active". controller
  publishes `/cmd_vel` (Twist) directly (no velocity_smoother/collision_monitor chain). Added a
  `smoother_server` config (SimpleSmoother, the BT's SmoothPath needs it). nav2 `odom_topic`→`/odom`.
- **Frontier explorer (works; debugged hard):** four real bugs fixed in `frontier_explorer.cpp` —
  (1) **DONE-latch**: it quit forever the first empty check; now waits for SLAM to populate a map
  (`mapHasFreeSpace`). (2) **ring-centroid**: a frontier ring around the robot has its centroid AT
  the robot → rejected by min_goal_distance; now targets a cluster cell, not the centroid.
  (3) **LiDAR blind spot**: I'd set the L1 FOV to ±135° → a phantom frontier always sat in the 90°
  rear blind wedge, so it perpetually chased its tail and stalled. **Fixed by making the sim LiDAR
  360°** (matches the real Unitree L1 — hemispherical 360°×90°; gz gpu_lidar ±π, 450 samples;
  pointcloud_to_laserscan ±π, 723 readings). (4) goal = nearest cluster cell **≥ min_goal_distance**
  (skip the near rear cells, don't reject the whole cluster). Result: robot drives origin→frontier,
  `/map` free cells grew 1882→14134 in one run.
- **VERIFIED LIVE:** `findFrontiers: free=… unknown=… frontierCells=… clusters=29`, `-> frontier
  (x,y)` goals sent, NavigateToPose drives the Go2, map expands. Full chain
  walk→LiDAR→SLAM→Nav2→frontier autonomous.

**EXPLORATION — big world + stall fixes (2026-06-25, in progress):**
- **New world `go2_worlds/worlds/facility.sdf`** (generated by `/tmp/gen_facility.py`): 30×20 m
  building, central corridor + **6 rooms behind 1.6 m doorways** + inspection crates/pillars. SDF
  validates ("Valid."); robot spawns at (0,0) corridor. This forces *real* exploration (rooms hidden
  beyond a single scan). Pass `world:=facility.sdf`. Chose a lightweight box world over a Fuel/AWS
  mesh world for machine-safety + zero porting (stack is world-agnostic, can swap later).
- **VERIFIED:** robot autonomously drove corridor → through a doorway → into the south room; `/map`
  free grew 1088 → **13297**. So exploration genuinely works in the big world.
- **Stall #1 (re-picks same frontier forever):** Nav2 reports SUCCEEDED once within ~goal-tolerance,
  and the explorer didn't blacklist on success → infinite re-pick. **Fixed:** blacklist EVERY
  attempted goal (success or fail) so it always advances; bumped `min_goal_distance` 0.5→0.8 (> goal
  tolerance, demands real travel).
- **Stall #2 (robot frozen, planner "compute_path aborting", behaviors "Collision Ahead"):** the
  360° LiDAR sees the dog's **own trunk/legs** (~0.2–0.4 m behind the front sensor) → a ring of
  phantom obstacles traps the planner. **Fixed:** LiDAR `range<min>` 0.15→**0.40** + p2l
  `range_min` 0.25→**0.45** (the real L1 has a dead zone too). ← about to test.

**Next:** full fresh run in facility (sim+slam → nav2 → frontier), confirm continuous multi-room
exploration; then rviz, remove debug logs, tune. Bring up INCREMENTALLY (all-in-one overloads the
machine). Targeted restarts: `/tmp/{slam,nav2,frontier}_kill.sh` (run isolated — self-kill trap).

---

## CHECKPOINT 8 — Full platform: odometry + SLAM working on the walking Go2 — 2026-06-25
**Status:** ✅ **walk → LiDAR → /scan → SLAM map** pipeline confirmed end-to-end.

- **Odometry (CONFIRMED):** re-enabled CHAMP `state_estimation_node` + 2 `robot_localization` EKFs.
  Fixes: EKF `imu0 livox/imu → /imu/data`; `publish_tf: True`; **`quadruped_controller.cpp` patched
  to publish gait-phase `foot_contacts` even in gazebo** (`!in_gazebo_` removed — no gz contact
  sensors, so the gait-phase estimate feeds `state_estimation`'s joint_states+foot_contacts sync →
  `odom/raw`). Result: `/odom` tracks motion, full TF `odom→base_footprint→base_link→…→utlidar_lidar`,
  `/foot_contacts` @200 Hz, `/utlidar/cloud_deskewed` @10 Hz. Leg+IMU odom (no gz ground truth = no
  cheat); SLAM corrects its drift. nav2 `odom_topic` → `/odom`.
- **System ABI fix (CONFIRMED):** the laptop ROS install was the April sync; `diagnostic_updater`
  4.2.7 (June) changed the `Updater` ctor ABI and broke April-built `controller_manager` +
  `robot_localization`. User ran full `sudo apt update && sudo apt upgrade` → all on June sync,
  `controller_manager`/`robot_localization`/`diagnostic_updater` now all `…EEdh`-consistent. No
  symbol errors; walking + EKFs both work.
- **SLAM (CONFIRMED):** `slam.launch.py` now uses `go2_champ` as the sim provider; p2l flattens
  `/utlidar/cloud_deskewed` → `/scan`, `slam_toolbox` (online_async, lifecycle) builds the map.
  **Key bug fixed:** Karto rejected EVERY scan ("contains 542, expected 543") because p2l's
  `ceil((max-min)/inc)` ≠ slam's `round()+1`; they agree only when `frac((max-min)/inc) ∈ (0,0.5)`.
  Set scan range ±2.355 (→541.38, frac .38) so both = 542. Now 0 warnings; **map saves with real
  walls (264 occupied) + free (2953) cells**, `map→odom` owned by slam.
- **Tooling:** added `/tmp/slam_kill.sh` (targeted kill of slam+p2l only, keeps the sim) — like
  `go2_kill.sh`, must be run as its own Bash call (self-kill trap: `pkill -f` matches parent argv).

**Next:** Nav2 bringup on the walking Go2 (costmaps from /scan, send a goal) → then the custom
`frontier_explorer` for fully autonomous mapping → realistic world + rviz.

---

## CHECKPOINT 7 — Real Go2 WALKS in Harmonic (CHAMP gait, omnidirectional /cmd_vel) — 2026-06-25
**Status:** ✅ **WALKING CONFIRMED.** Real Go2 mesh model stands rock-solid (pose identical over 8 s,
z=0.245, roll/pitch≈0) and walks from `/cmd_vel`: forward ~0.14 m/s @ vx=0.15 (accurate), in-place
rotate ~0.46 rad/s @ vyaw=0.5, strafe on vy — staying upright throughout. The fix that cracked it:
**effort+PID → POSITION command interface (ADR-011)** + stand-pose-at-spawn + comprehensive teardown.

Built `go2_champ.launch.py` (gz Harmonic + RSP + spawn + ros_gz bridge + controller spawners +
champ gait) and adapted the copied CHAMP for Harmonic. Bugs found & fixed, in order:

1. **`no match for platform` / space-in-path** — the workspace path "EE26 Hackathon" has a space →
   `Command(["xacro ", file])` shlex-splits wrong. Fixed: process xacro via `subprocess.run(["xacro",
   file])` and write `/tmp/go2_gz.urdf`.
2. **`gz_ros2_control-system` "Could not find shared library"** — gz can't see the ROS lib dir.
   Fixed: add `/opt/ros/jazzy/lib` to `GZ_SIM_SYSTEM_PLUGIN_PATH` in the gz `additional_env`.
3. **Duplicate Classic `ros2_control` blocks** — `leg.xacro` emits a per-leg
   `gazebo_ros2_control/GazeboSystem` (Classic) block. Added macro param `classic_ros2_control`
   (default true so the Classic `robot.xacro` is unchanged); `robot_gz.xacro` passes `false`. URDF
   now has exactly 1 (gz) ros2_control block, 12 revolute joints.
4. **champ `bringup.launch.py` crashed the whole launch** — eager `FindPackageShare("champ_config")`
   (pkg we don't have) → removed (we pass go2_config paths as args). Also dropped its duplicate
   `robot_state_publisher` (we run our own), and wrapped the `urdf` param in
   `ParameterValue(Command(...), value_type=str)` (else launch parses the URDF XML as YAML → throws →
   SIGINT to gz → dog falls).
5. **Dog spawned then collapsed onto its back** — ~9 s gap between spawn and gait (limp legs under
   gravity). Fixed: seed STAND pose at spawn via gz_ros2_control `initial_value` (hip 0, thigh 0.9,
   calf −1.8), spawn z=0.275, tightened staggering (spawn 3 s → controllers 5–6 s → gait 8 s).
6. **Chaotic walking + falls** — two causes: (a) the teardown script didn't kill `quadruped_controller`
   /champ nodes, so a **second gait controller** from a prior run fought over the joints → rewrote
   `/tmp/go2_kill.sh` to be comprehensive (champ/quadruped/state_estimation/ekf/slam/etc.); (b) the
   **effort+PID** joint control (gains tuned for Classic/ODE) **oscillates on Harmonic/DART** even at
   zero `/cmd_vel` → **ADR-011: switched to a POSITION command interface** (gz's stable internal joint
   controller; no ROS-side PID). Sim-only — real Go2 still uses `/cmd_vel`→SportClient (ADR-002/007).

**Tooling lesson (reaffirmed):** never combine `bash /tmp/go2_kill.sh` with a literal
`ros2 launch go2_bringup …` in the *same* Bash command — the kill's `pkill -f` matches the parent
shell's own argv and self-kills it (silent exit 1, no output). Keep teardown in its own call.

**Next:** supervised launch — confirm it stands still cleanly then walks from `/cmd_vel`; then
re-enable champ `state_estimation` + EKFs (odom→base_link TF for SLAM/Nav2), add realistic world,
re-point the autonomy stack.

**ODOM RE-ENABLE (2026-06-25, in progress):** re-enabled `state_estimation_node` + the two
`robot_localization` EKFs in `champ_bringup/bringup.launch.py`; fixed EKF `imu0: livox/imu →
/imu/data` (our bridged gz IMU), set `publish_tf: True`, and set `publish_foot_contacts: true`
(state_estimation syncs joint_states+foot_contacts → `odom/raw`). `state_estimation_node` runs and
publishes `/odom/raw` + `/foot_contacts` ✓. **BLOCKER:** both EKF nodes die instantly —
`librl_lib.so: undefined symbol … diagnostic_updater::Updater(...)`. Root cause = **apt version
mismatch**: `ros-jazzy-robot-localization` 3.8.3 (rebuilt 2026-06-15) needs
`ros-jazzy-diagnostic-updater` **4.2.7**, but 4.2.6 is installed (4.2.7 is the apt candidate).
Using leg+IMU odometry (not gz ground-truth) = no cheat, mirrors the real Go2's onboard odom.

**UPDATE — wider ABI break (the diagnostic_updater upgrade rippled):** installing
`diagnostic-updater` 4.2.7 (June sync) fixed robot_localization but **broke `controller_manager`**
(`libcontroller_manager.so: undefined symbol …Updater(…)EEd`). Root cause: the whole ROS install
was the **2026-04-12** sync (336 ros-jazzy pkgs upgradable), but 4.2.7 is **2026-06-15** — its
`Updater` ctor ABI changed (now only the `…EEdh` overload), so every April package built against
the old `…EEd` ABI (controller_manager, controller-interface, ros2-controllers, gz-ros2-control,
and likely slam_toolbox/nav2) breaks. **Resolution (user chose, running):
`sudo apt update && sudo apt upgrade`** → bring all ROS pkgs to the consistent June sync. After it
completes: relaunch, verify EKFs + `/odom` + `odom→base_footprint→base_link` TF, then SLAM/Nav2.

---

## CHECKPOINT 6 — Pivot to REAL walking Go2 via CHAMP; CHAMP compiles on Jazzy — 2026-06-25
**Status:** 🟡 real Go2 CHAMP in + compiling; Classic→Harmonic adaptation next.
**Decision:** ADR-010 (user wants the actual walking Go2 like the repos, not the box).

- Replaced the box velocity-base with the **complete Go2 CHAMP setup from
  `Go2_planner_suite/unitree-go2-ros2`** (the proven walking-Go2 config): `champ`, `champ_base`
  (gait), `champ_msgs`, `champ_description`, **`go2_config`** (Go2 gait/joints/links/ros_control),
  **`go2_description`** (real Go2 URDF + dae meshes). Joint naming = CHAMP `lf/rf/lh/rh`.
  (Lean box description backed up to `src/.go2_description_lean_backup`.)
- **CHAMP COMPILES CLEAN ON JAZZY** — `champ_base` (the C++ gait controller) built in 16.5 s.
  Biggest feasibility risk retired.
- Confirmed the repo is Gazebo **Classic** + Humble; on our Jazzy+Harmonic we swap the
  integration: `gz_ros2_control` (installed ✓; needs `ros-jazzy-ros2-control`) + gz sensors
  (gpu_lidar=L1→`/utlidar/cloud_deskewed`, gz imu, gz OdometryPublisher→`/utlidar/robot_odom`).
  Keep URDF kinematics + meshes + the 12-joint ros2_control block.

**BLOCKER (user):** `sudo apt install ros-jazzy-ros2-control` (the controller_manager hardware
iface for gz_ros2_control). Everything else is installed.
**Next:** write `go2_gz.urdf.xacro` (Harmonic gz_ros2_control + gz sensors) + gz `ros_control.yaml`
+ walking launch (gz + spawn + joint_state_broadcaster + trajectory controller + champ_base) →
verify Go2 WALKS from `/cmd_vel` (SUPERVISED — heavy launch), then re-point SLAM/Nav2/frontier.

---

## CHECKPOINT 4+5 (code complete) — Nav2 + frontier exploration built & compiling — 2026-06-25
**Status:** 🟡 code complete + compiles; full Gazebo run pending user return (machine-safe).
**Stage:** 1, steps 6–8.

- **Nav2** (`go2_bringup/config/nav2_params.yaml` + `launch/nav2.launch.py`): reuses
  `nav2_bringup/navigation_launch.py` (controller DWB / planner NavFn / behaviors / BT /
  velocity_smoother + lifecycle); **no AMCL** (slam owns map→odom); **allow_unknown: true**
  (plan toward frontiers); costmaps from `/scan`; Go2 footprint 0.40×0.24; vx≤0.3, lean for Orin.
- **Frontier exploration** (`go2_exploration/src/frontier_explorer.cpp`, C++): subscribes
  `/map`, detects frontiers (free cells adjacent to unknown) → BFS-cluster → nearest navigable
  → Nav2 `NavigateToPose`; FSM IDLE/EXPLORING/NAVIGATING/DONE; per-goal timeout + blacklist;
  `/explore/start`|`/stop` services; RViz frontier markers; autostart. **Compiles clean (8.8s).**
  Depends only on `/map` + Nav2 action + TF → simulator-agnostic, ports to real Go2 (ADR-002/007).
- **`launch/explore.launch.py`**: full Stage-1 — sim+SLAM → (t+10s) Nav2 → (t+18s) frontier.

All four packages build. SLAM proven working (CP3). **REMAINING:** run
`explore.launch.py headless:=true` under supervision → confirm the Go2 autonomously maps the
lab world (populated `/map`, frontier goals, exploration completes), then `map_saver_cli`.
Watch RAM (14 GB) — close Firefox/spare apps first; one sim at a time.
**Status:** 🟡 SLAM fix proven (no-Gazebo isolation); full Gazebo map-build verification
pending user return (machine-safe mode). **Stage:** 1, step 5.

Built `go2_bringup` SLAM layer: `config/pointcloud_to_laserscan.yaml` (L1 cloud→/scan),
`config/slam_toolbox.yaml` (2D online-async, lean), `launch/slam.launch.py`.

**Three bugs found & fixed (all evidence-driven, no guessing):**
1. **Zombie processes** — my `kill` killed only the launch wrapper, not its child nodes;
   9+ leftover `parameter_bridge`/`slam_toolbox` accumulated → multiple `/clock` publishers
   at different sim-times → 1693× "jump back in time, clearing TF buffer". Fix: process-group
   kill + PID-excluded `pkill` run from a script file (so the pattern doesn't match my own shell).
2. **use_sim_time not binding** — yaml node-name key mismatch → nodes ran wall-time vs TF
   sim-time. Fix: `/**` wildcard + explicit `use_sim_time` in launch.
3. **slam_toolbox is a LIFECYCLE node** (THE blocker) — a plain `Node` leaves it
   UNCONFIGURED so it never subscribes to `/scan` (0 subscribers, no log). Fix: `slam.launch.py`
   now **includes slam_toolbox's stock `online_async_launch.py`** (which does configure+activate)
   with our params. Also removed `stack_size_to_use` (spawned a custom-stack thread that hung
   headless). **Proven (no Gazebo, fake scan):** subscription count 1, `map→odom` published,
   "Registering sensor: [Custom Described Lidar]".

**Machine-safety incident:** repeated headless-Gazebo launches + zombies on a **14 GB RAM**
laptop (was 10 GB used + swap) + GPU/EGL stress likely froze the desktop. Mitigations: never
leave zombies (process-group teardown), one sim at a time, **lightened the LiDAR 720×16→360×8**
(less GPU, leaner for Orin), and debugging SLAM **without Gazebo** via a synthetic-scan harness.

**Next:** Nav2 (CP4) + frontier exploration node (CP5) — building as code now; full Gazebo
SLAM+Nav2+exploration verification when the user is back to babysit it.

---

## CHECKPOINT 2 — Go2 in Gazebo, mirrored topics, drivable — 2026-06-24
**Status:** ✅ green (verified headless). **Stage:** 1, steps 2–4.

Built & tested the sim vertical slice:
- `go2_worlds/worlds/lab.sdf` — enclosed 8×8 m room + 2 obstacles + gz systems
  (physics/sensors/scene-broadcaster/contact/imu).
- `go2_description/urdf/go2.urdf` — real Go2 trunk dims + **L1 LiDAR** (gpu_lidar, front-biased
  ±135° H × ±30° V, 720×16, frame `utlidar_lidar`) + **velocity base** (gz VelocityControl) +
  **OdometryPublisher** + IMU. Legs/meshes deferred (cosmetic, next).
- `go2_bringup/config/ros_gz_bridge.yaml` + `launch/sim.launch.py`.

**Verified (headless):** the **mirrored real-Go2 topics are live** — `/utlidar/cloud_deskewed`
(PointCloud2 16×720, `utlidar_lidar`), `/utlidar/robot_odom` (Odometry, `odom`→`base_link`),
`/cmd_vel` drives it (x 0→1.08, y 0→0.55, yaw 0.965 on a fwd+yaw cmd), `/imu`, `/clock`, full
TF `odom→base_link→{utlidar_lidar,imu_link}`.

**Design note / decision:** Stage-1 velocity base is **kinematic — gravity disabled on
`base_link`** so the legless trunk floats at standing height while VelocityControl drives
x/y/yaw (consequence of ADR-003). Gravity + a real gait controller return in Phase 2.
**Evidence-driven fix:** first run sank z 0.40→0.20 under gravity; gravity-off fixed it
(verified z holds 0.40 while driving).

**Gotcha logged:** headless gz prints `libEGL ... failed to create dri2 screen` warnings —
**non-fatal**, the GPU lidar still renders a full cloud (RTX 5060). `gz_frame_id` "not defined
in SDF" warning is also benign (frame_id arrives correct).

**Next (Checkpoint 3):** SLAM — `pointcloud_to_laserscan` (/utlidar/cloud_deskewed → /scan) +
`slam_toolbox` online-async → live `/map` + `map→odom`. Then Nav2 (CP4), frontier (CP5).

---

## CHECKPOINT 1 — Stage-1 workspace scaffolded — 2026-06-24
**Status:** ✅ structure ready (content + build pending deps). **Stage:** 1, steps 1–2 setup.

- Created `go2-sim/go2_ws` colcon workspace with 4 packages: `go2_description`,
  `go2_worlds`, `go2_bringup`, `go2_exploration` (package.xml + CMakeLists + dirs).
- Wrote `go2-sim/scripts/install_deps.sh` — the Jazzy apt stack (ros_gz, slam_toolbox,
  navigation2/nav2_bringup, pointcloud_to_laserscan, robot_localization, twist_mux, xacro,
  rviz2). **Needs the user's sudo to run** (the one gating step).
- Local assets available to populate `go2_description`: Go2 URDF + DAE meshes already cloned
  at `../simulation/quadruped-dog-rl/urdf/go2_unitree/`; official Menagerie Go2 in the
  robot_descriptions cache (MJCF, reference only).

**BLOCKER (user action):** run `go2-sim/scripts/install_deps.sh` (sudo) — then I build
Stage-1 content (Go2 SDF+LiDAR, world, ros_gz bridge, slam_toolbox, Nav2, frontier node).

**Next (Stage 1, step 2):** assemble `go2_description` (Go2 xacro→SDF + gz LiDAR/IMU + a
`/cmd_vel` velocity base) and a first `sim.launch.py` (gz + robot + bridge + RViz).

---

## CHECKPOINT 0 — Foundation decided & documented — 2026-06-24
**Status:** ✅ complete. **Stage:** 0 → entering Stage 1.

- Deep-analyzed 4 reference repos (Go2-Inspector, exploration_go2, Go2_planner_suite,
  isaac-go2-ros2) → `docs/00-REFERENCE-REPOS.md`. Verdict: all ROS 2, none on our stack;
  their autonomy logic is sim-agnostic and portable. Primary reuse: Go2-Inspector's
  `frontier_explorer.cpp` + inspection/report; exploration_go2's info-gain explorer + 3D→2D
  projector; Go2_planner_suite's TARE/terrain (later).
- Audited environment: Ubuntu 24.04, ROS 2 **Jazzy**, Gazebo **Harmonic 8.11**, RTX 5060 8 GB.
  **All autonomy packages available as Jazzy apt binaries** (slam_toolbox, navigation2,
  ros_gz, rtabmap_ros, pointcloud_to_laserscan, robot_localization). `explore_lite` NOT
  packaged → custom frontier node.
- **Decisions recorded** (ADR-001..006): **Gazebo Harmonic + ROS 2 Jazzy**; sim-agnostic
  autonomy stack; decouple locomotion from SLAM; slam_toolbox 2D→RTAB-Map 3D; custom
  frontier node; keep MuJoCo as locomotion track.
- Wrote roadmap (Stages 0–4) + topic/TF contract.

**Next (Stage 1, step 1):** create `go2_ws` colcon workspace + `apt install` the Jazzy
stack; then official Go2 SDF + LiDAR in Gazebo Harmonic.

**Open risks:** (1) official Go2 URDF→SDF + gz sensors on Harmonic needs care; (2) Go2
legged locomotion deferred (ADR-003) — Stage-1 uses a velocity base; (3) 8 GB VRAM caps
Phase-3 perception (lean models / offload to Claude).

**CHECKPOINT 14 (2026-06-25): ROOT-CAUSE FIX for obstacle avoidance — self-filter + MPPI + recovery (ADR-013).** Symptom (warehouse + facility): robot moves a little then FREEZES, controller logs "No valid trajectories out of 440!" + "Collision Ahead - Exiting Spin", though Gazebo shows it physically clear & upright (z=0.245). **Root cause (the real one):** the 360° LiDAR's large self-occlusion dead zone (lidar range_min 0.40 / p2l 0.45, added to hide the dog's own legs) forces a lose-lose — WITH static_layer in the local costmap a wall the robot approaches enters the dead zone (so /scan can't CLEAR it) yet stays lethal under the footprint → DWB freezes; WITHOUT static_layer the robot can't see close walls → climbs them. Confirmed via clean /scan parse: min return 0.72m, ZERO returns <0.7m (no self-hit ring — the dead zone is purely hiding close real walls). **Three fixes:** (1) **self_filter C++ node** (`go2_exploration/src/self_filter.cpp`): subscribes /utlidar/cloud_deskewed, transforms each point to base_link via TF, drops points inside the robot body box (x[-0.40,0.40] y[-0.26,0.26] z[-0.50,0.40]), republishes /utlidar/cloud_filtered — removes self-hits GEOMETRICALLY so the dead zone can shrink to 0.15m (lidar range_min 0.40→0.15, p2l 0.45→0.15); p2l + octomap now consume the filtered cloud; runs on the REAL Go2 too (same L1 self-occlusion). (2) **DWB → MPPI controller** (nav2_params.yaml): MPPI samples 2000 rollouts and always follows the least-cost one (never the DWB hard-freeze), CostCritic(consider_footprint:true) = smooth footprint-exact avoidance in tight aisles; limits match the trot (vx≤0.26, wz≤0.5, DiffDrive, gentle accel). (3) **Recovery behaviors** (behavior_server): spin + backup + drive_on_heading + wait (BT recovery subtree fires them; reusable on real hardware; wait handles the warehouse's walking people). Costmap inflation 0.16/2.0 → 0.25/3.0 for a smoother MPPI gradient. **STATUS: code/config complete; pending build (self_filter node + verify ros-jazzy-nav2-mppi-controller installed) + warehouse test.** NEXT: colcon build go2_exploration + go2_description + go2_bringup → full restart in warehouse → confirm robot avoids obstacles & explores without freezing.

**CHECKPOINT 15 (2026-06-25): ✅ ROBUST 2D AUTONOMY RESTORED + HARDENED (ADR-013 confirmed). Dropped 3D for now (user call).** CHECKPOINT 14's fixes are now VERIFIED working end-to-end. Per user ("stick with 2D, the 2D was perfect, make it proper and robust") the 3D octomap is REMOVED from the demo (it was viz-only, never touched nav, but it added load/clutter; revisit later). The hardened 2D stack = slam_toolbox (2D localization+map) + self_filter + nav2(MPPI) + frontier(clearance) — all sim-agnostic for the real Go2. **Three root-cause fixes that made it robust:** (1) **self_filter** (go2_exploration/src/self_filter.cpp) removes the dog's OWN body from the L1 cloud geometrically → LiDAR dead zone shrunk 0.40→0.15m → walls are SEEN+CLEARED next to the robot (fixes the static_layer wall-trap AND wall-climbing — the lose-lose was the big dead zone); verified clean scan (0 self-hit returns in all 4 directions at the open spawn). (2) **DWB→MPPI** — DWB hard-froze "No valid trajectories out of 440" the instant the footprint touched an inflated/static cell; MPPI samples 2000 rollouts, never freezes, CostCritic(consider_footprint) = smooth footprint-exact avoidance. MPPI gotcha: controller_frequency must give period<=model_dt → set 20Hz (period 0.05 = model_dt 0.05). (3) **frontier wall-clearance** — explorer was picking the nearest frontier CELL with no wall-clearance check → waypoints landed in/against walls → robot drove up, couldn't reach, recovered(backup), retried = front-and-back oscillation + "trying to align but not going"; FIX: goal cell must have >=goal_clearance(0.30m=robot half-diag+margin) from any occupied cell (unknown cells don't count). Plus recovery behaviors spin+backup+drive_on_heading+wait (BT recovery subtree, real-HW-ready), inflation 0.16/2.0→0.25/3.0 (smooth MPPI gradient). **✅ VERIFIED in facility (headless gz + GUI + RViz):** from origin (0,0) the Go2 explored corridor + ENTERED a north room (y=2.47 through the 3m doorway) + drove corridor west to x=-9; /map free 73758→116753; 6 goals; novalid=0 (zero freezes); upright z≈0.245 throughout; no wall-hit/oscillation. **RUN (clean 2D, incremental):** `slam.launch.py with_sim:=true headless:=true world:=facility.sdf` (spawn 0,0; world:=warehouse.sdf spawn_x:=-8 spawn_y:=0 for warehouse) → `nav2.launch.py use_sim_time:=true` (wait ~30s 'Managed nodes are active') → `ros2 run go2_exploration frontier_explorer -p use_sim_time:=true -p autostart:=true -p robot_base_frame:=base_link -p goal_clearance:=0.30` → `rviz2 -d .../explore.rviz` + `gz sim -g`. NOTE: octomap.launch.py still exists for when we re-add 3D. Debug RCLCPP_INFO still in frontier_explorer.cpp (cleanup pending). Self-kill trap reminder: never run a kill script in the same Bash cmd as text matching its pkill -f patterns.

**CHECKPOINT 16 (2026-06-25): ✅ REVERTED TO SIMPLE ROBUST STACK — DWB + custom frontier + facility (user call: "use the simple DWB stack, don't overcomplicate, revert to perfect 2D mapping using frontier").** After exploring fancier options this session, reverted to the proven CHECKPOINT-10 design but keeping the genuine under-the-hood fixes. **What we tried + learned (ADR-013/014):** (a) self_filter node — removes the dog's own body from the L1 cloud so the LiDAR dead-zone shrinks 0.40→0.15m (KEPT — clean scan, no self-hit ring, fixes the static-trap/wall-climb lose-lose); (b) gz OdometryPublisher position-fused — ~5cm localization, no drift (KEPT — prevents wall-climbing); (c) frontier wall-clearance + reactive arrival/progress backstops in frontier_explorer.cpp (KEPT — no goals-in-walls, no circling); (d) MPPI controller — REVERTED (wandered off the planned path); (e) explore_lite/m-explore-ros2 — built for Jazzy (Ubuntu 24.04 Noble, compat verified), works on the global costmap (needs always_send_full_costmap:true since its costmap_client subscribes volatile), but markers too big + REVERTED per user; (f) RPP — REVERTED. **FINAL DEMO STACK = slam_toolbox(2D) + self_filter + nav2(DWB) + custom frontier_explorer.** DWB tuned: vtheta_samples 40 (always a valid in-place turn → no "no valid trajectories" freeze), vx≤0.26, wz≤0.5, critics RotateToGoal/Oscillation/BaseObstacle/Goal+Path Align/Dist. goal_checker yaw_goal_tolerance 3.15 (ignore final heading → no end-of-goal circling) + xy 0.35. **✅ VERIFIED in facility (gz GUI + RViz):** from origin the Go2 explored corridor → SOUTH ROOM (y=-6.8 through 3m doorway) → back; /map free 2108→97084; 15 goals; novalid=0 (DWB never froze); upright z≈0.245; no circling/wall-hits. **RUN (simple 2D demo):** `slam.launch.py with_sim:=true headless:=true world:=facility.sdf` → `nav2.launch.py use_sim_time:=true` (wait ~30s active) → `ros2 run go2_exploration frontier_explorer -p use_sim_time:=true -p autostart:=true -p robot_base_frame:=base_link -p goal_clearance:=0.30` → `rviz2 -d .../explore.rviz` + `gz sim -g`. NOTE: warehouse.sdf (tb4) is tight for the Go2 footprint+inflation → planner struggles there; facility is the solid foundation. explore_lite kept in workspace (src/m-explore-ros2) for future. Self-kill trap reminder: nav2_kill.sh pkills 'ros2 launch go2_bringup nav2' → never run it in the same Bash cmd that also launches nav2.

**CHECKPOINT 17 (2026-06-25): sensors made real-Go2-accurate — LiDAR at the mouth (downward), front camera added, footprint fixed for leg clearance.** Per user feedback after watching the gz GUI: (1) **LiDAR relocated** from floating above the body (trunk+0.20,0,+0.10) to the **MOUTH/chin** (trunk+0.30,0,-0.02) **tilted 8.6deg downward** (rpy 0 0.15 0) — mirrors the real Go2 L1 placement; vertical FOV widened ±0.55->±0.62 + 16->18 rings so forward walls stay covered despite the tilt. Verified base_link->utlidar_lidar = (0.30,0,-0.02) pitch 8.6deg, /scan still 342 finite returns, no self-hits. (2) **Front CAMERA added** (the model had NONE): camera_link at trunk+(0.30,0,0.06) + gz camera sensor 640x480 R8G8B8 @15Hz in gazebo_gz.xacro, bridged gz /camera/image -> ROS /camera/image_raw in ros_gz_bridge_champ.yaml. VERIFIED working — captured a real frame (mean brightness 170/255, all 307200 px non-black, shows the facility corridor). Feeds the future inspection/detection task. (3) **Costmap footprint enlarged** 0.52x0.32 -> 0.60x0.40 ([[0.30,0.20]...]) in both costmaps so the LEG-SWING CORNERS are covered (fixes the leg clipping a doorway corner the user saw). **Clarified for user: there is ONE functional LiDAR (the L1/utlidar) — the "second lidar" seen in the GUI is the Go2 BODY MESH's molded head sensor housing (cosmetic, part of trunk.dae), not a working sensor.** VERIFIED full stack still robust after changes (facility + DWB + custom frontier): origin->south room, /map free 2219->39609, 9 goals, novalid=0 (no DWB freeze), upright z~0.245. Camera frame saved to scratchpad/go2_camera.png. To SEE the camera live in RViz: add an Image display on /camera/image_raw.

**CHECKPOINT 18 (2026-06-25): ROBUSTNESS pass — clearance + gentler turns + info-gain frontier + reverse recovery.** Targeted the two user-reported symptoms (rear legs clipping walls -> tipping; "getting stuck, not knowing where to go"). FOUR fixes: (1) **Clearance** — global+local costmap `inflation_radius` 0.25->0.35 and `footprint_padding` 0->0.05 (with the 0.60x0.40 leg-covering footprint) so the planned path keeps the WHOLE leg envelope off walls; this is the real anti-tip lever since the open-loop trot can't recover from wall contact. (2) **Gentler turns** — DWB `max_vel_theta` 0.5->0.4 (less rear-leg sweep + tip energy). (3) **Info-gain frontier selection** (frontier_explorer.cpp) — score = gain_scale*clusterSize - potential_scale*dist (explore_lite-style, gain 1.0 / potential 3.0): prefer LARGE frontiers penalised by distance instead of pure nearest -> purposeful "where to go", less dithering. (4) **Reverse-then-replan recovery** — new REVERSING state + /cmd_vel publisher: when the progress watchdog detects a wedge (no motion in 12s) the explorer cancels, backs up ~0.24 m (reverse_speed 0.12 x 2.0s; the Go2 walks in reverse, gentler than spinning in a tight spot), then replans; plus **blacklist TTL** (30s) so failed frontiers are retried once the map fills in (no permanent give-up). **VERIFIED facility (DWB + custom frontier, gz GUI + RViz):** origin -> east -> NORTH ROOM (y=2.98) -> south; /map free 2219->84185; 7 goals; 1 reverse-recovery (fired + recovered, kept exploring); novalid=0 (no DWB freeze); upright z~0.245 throughout (no tip). Sensors per CP17 (lidar at mouth/downward, camera on face). RUN unchanged: slam.launch.py world:=facility.sdf -> nav2.launch.py -> frontier_explorer (params now default to info-gain + reverse recovery) -> rviz + gz sim -g.

**CHECKPOINT 19 (2026-06-25): 2D backed up + 3D mapping layered on top (additive, 2D untouched).** Per user ("2D is perfect, save as backup, now add 3D for full lidar data"). (1) **BACKUP** of the working 2D source -> `go2-sim/backups/2D-stack-WORKING-2026-06-25.tar.gz` (13MB: go2_bringup/go2_exploration/go2_description/go2_config/champ_base — all configs/launches/source/xacros). Restore: `tar xzf <tarball> -C go2_ws/src`. (go2_ws is NOT a git repo.) (2) **3D = octomap_server** (`octomap.launch.py`, already present) run ALONGSIDE the 2D stack — consumes the SAME `/utlidar/cloud_filtered` the 2D p2l uses, so the 2D /scan/SLAM/Nav2 are 100% unaffected. Publishes the 3D occupancy map: `/occupied_cells_vis_array` (colored-by-height voxels @10Hz), `/octomap_point_cloud_centers` (@10Hz), `/octomap_full` (octree). VERIFIED: 13729 occupied voxels accumulating while the 2D robot keeps exploring (pose moving, /scan 10Hz). explore.rviz already has the octomap MarkerArray display (shows the colored voxels, no rviz change needed). **Full 3D data available to the user:** raw per-frame 3D cloud (/utlidar/cloud_deskewed), accumulated 3D map (/occupied_cells_vis_array + /octomap_point_cloud_centers), saveable octree (/octomap_full -> `ros2 run octomap_server octomap_saver -f map.ot`). **RUN (2D + 3D):** the usual 2D bringup (slam.launch.py world:=facility.sdf -> nav2.launch.py -> frontier_explorer -> rviz) THEN `ros2 launch go2_bringup octomap.launch.py use_sim_time:=true`. octomap resolution 0.10m, range 4.5m. NEXT (optional): densify lidar vertical rings (18->32) + widen FOV for a fuller/denser 3D map (the 2D /scan is flattened so it's unaffected) — offered to user.

**CHECKPOINT 20 (2026-06-25): LiDAR set to mirror the real Unitree L1 (360x90 deg) + live 3D construction.** Confirmed from Unitree docs: the real Go2 L1 has NO fixed rings — it's a dual-motor non-repetitive scanner, 360 deg H x 90 deg V hemispherical, ~21,600 pts/s, min range 0.05m (sources in chat). gz gpu_lidar is ring-based, so we approximate: vertical FOV ±0.62->±0.785 rad (90 deg total) + 18->32 rings (gazebo_gz.xacro). Cloud now 32x450=14,400 pts/frame @10Hz (~144k pts/s, denser than the real L1 — fine for sim). 2D /scan UNAFFECTED (p2l flattens regardless of ring count) — verified still 10Hz, robot upright, nav fine. **Live 3D run (2D+3D together, facility):** octomap grew 1368->23662 voxels as the robot explored corridor+rooms; 2D nav 6 goals, novalid=0, upright throughout; mem ~6GB free, load ~8 (moderate; the Orin handles L1-class natively). Full 3D data: /utlidar/cloud_deskewed (raw 3D), /occupied_cells_vis_array (colored voxels), /octomap_point_cloud_centers, /octomap_full (save via octomap_saver -f map.ot). RViz explore.rviz shows the voxels (Fixed Frame map). 2D backup safe at backups/2D-stack-WORKING-2026-06-25.tar.gz. RUN: slam.launch.py world:=facility.sdf -> nav2.launch.py -> frontier_explorer + octomap.launch.py -> rviz + gz sim -g.

**CHECKPOINT 21 (2026-06-25): shareable package created.** Built a self-contained, distributable copy for a teammate: `EE26 Hackathon/go2-autonomous-inspection-sim/` (+ `.zip`, 14MB). Contents: all 10 ROS2 packages (5 CHAMP + 5 go2, full source incl. meshes/worlds/configs/launches), a complete README.md (prereqs Ubuntu24.04+Jazzy+Harmonic, apt+rosdep deps, colcon build, run commands, worlds, sensors, topics, architecture, troubleshooting, deployment context), scripts/ (run.sh one-command staged bringup [2d|3d] [world], stop.sh teardown, gen_facility.py), docs/PROGRESS-LOG.md. EXCLUDED: build/install/log, m-explore-ros2 (unused; demo uses custom frontier_explorer), and a stray `.go2_description_lean_backup` (had a duplicate package.xml — caught + removed so colcon won't choke). Also removed dangling explore_lite/rtabmap launches (reference excluded/uninstalled pkgs). Verified: exactly 10 package.xml, 0 stray entries, all key files present.

**CHECKPOINT 21b (2026-06-25): friend's package — removed the octomap 3D-MAP, kept the raw 3D scan.** Per user (friend only wants the 3D scan, not the octomap mapping; keep octomap for us). In the PACKAGE ONLY (go2-autonomous-inspection-sim/): deleted octomap.launch.py; swapped the RViz octomap MarkerArray -> a PointCloud2 display on /utlidar/cloud_deskewed (the raw 3D LiDAR scan, AxisColor Z); rewrote run.sh (no 3d/octomap mode, just `run.sh [world]`) + README (no octomap, documents the raw 3D scan + how to view it). Verified: package has 0 octomap refs (launch/README/run.sh/rviz), 10 packages, raw 3D scan present. OUR working copy (go2-sim/go2_ws) STILL has octomap.launch.py + the octomap RViz display — untouched. Re-zipped: go2-autonomous-inspection-sim.zip (14M).

**CHECKPOINT 22 (2026-06-25): FIX — removed the blind /cmd_vel reverse recovery; Nav2 now owns all motion+recovery (collision-aware).** User observed the robot reversing INTO obstacles behind it + getting stuck, and asked to verify whether reverse-in-exploration is done right. RESEARCH VERDICT (Nav2 docs + issue #2189): Nav2's BackUp recovery is COLLISION-AWARE (simulate_ahead_time checks the costmap, stops on "Collision Ahead"); the standard frontier explorers (explore_lite/m-explore) NEVER publish /cmd_vel — they only send NavigateToPose and let Nav2 own motion+recovery. Our CHECKPOINT-18 reverse recovery published /cmd_vel vx=-0.12 DIRECTLY for 2s with NO collision check = a blind reverse that hit obstacles behind the robot (the user's bug). FIX (frontier_explorer.cpp): removed the REVERSING state + reverseTick() + startReverse() + cmd_vel_pub_ + reverse_timer_ + reverse params + the twist include. On "no progress (stuck)" -> advance() (cancel + blacklist + replan to a DIFFERENT frontier); Nav2's own collision-aware BackUp/Spin do any backing-up safely within the goal first (progress_timeout 12->18s so Nav2's recovery runs before we give up). The robot CAN still go backward — via Nav2 (DWB min_vel_x -0.12, collision-checked; + BackUp recovery) — just never blindly. Built clean. (Warehouse nav tuning from CP21: inflation 0.35->0.25 + footprint 0.60x0.40->0.52x0.32 so it fits the tight tb4 aisles — it spread east to (7,-4), 53k 3D voxels; the path-close-to-obstacles the user noted is the low inflation, but DWB's footprint check + collision-aware recovery mean it won't actually hit.) NEXT: run warehouse to confirm no more reverse-into-obstacle.

**CHECKPOINT 23 (2026-06-25): RTAB-Map stack (ADR-015) -- Phase 1 in progress. Frontier/slam_toolbox stack PAUSED + untouched.** New SEPARATE stack per user request: graph-SLAM localization + 3D mapping fusing 4D LiDAR + RGB camera + accurate /odom, integrated with Nav2. New file go2_bringup/launch/rtabmap_slam.launch.py (reuses go2_champ + self_filter + p2l; runs rtabmap_slam/rtabmap instead of slam_toolbox; replaces the stale CP11 rtabmap.launch.py). Key params: subscribe_scan_cloud(/utlidar/cloud_filtered) + subscribe_rgb(/camera) + external /odom, approx_sync, Reg/Strategy=1(ICP), Reg/Force3DoF=true (the CP11-drift fix), RGBD/ProximityBySpace=true (LiDAR loop closure for the low-texture sim), Grid/Sensor=1 (grid from cloud projection), Rtabmap/DetectionRate 2Hz (Orin-lean). Gotchas fixed: (a) rtabmap must start AFTER the CHAMP EKF or it loses odom at startup -> TimerAction delay 14s; (b) Grid/Sensor defaulted to 0 (laser) -> set 1 (cloud) so /map builds. **VERIFIED working: rtabmap localizes (map->odom), 2D grid /map=602 cells, pose graph /rtabmap/mapGraph = 128 nodes + 2 LiDAR-proximity loop closures, ~5cm drift (visual loop closure rejected -- low-texture facility, but proximity/ICP carries it + accurate odom).** REMAINING Phase 1: the 3D cloud outputs (/rtabmap/cloud_map, cloud_obstacles, octomap_*) are EMPTY -> fixing with Grid/3D=true (build 3D occupancy). Jetson confirmed 16GB RAM (not 8). RUN: `ros2 launch go2_bringup rtabmap_slam.launch.py world:=facility.sdf`. NOTE: this is ADDITIVE -- the slam_toolbox+frontier 2D stack (slam.launch.py/nav2.launch.py/frontier_explorer) is the untouched backup.

**CHECKPOINT 24 (2026-06-25): RTAB-Map + Nav2 INTEGRATION working (Phases 1+2). ADR-015.** rtabmap_slam.launch.py now fully wired: rtabmap localizes (map->odom, clean ~few cm, z-locked by Force3DoF) + publishes the 2D grid + pose graph (50+ nodes + LiDAR-proximity loop closures); octomap_server builds the 3D map (17k+ voxels) in rtabmap's corrected frame; Nav2 consumes rtabmap's /map + map->odom (verified: global_costmap StaticLayer received rtabmap's map). KEY FIXES this session: (1) rtabmap's 2D occupancy grid topic is 'map' (->/rtabmap/map), NOT 'grid_map' -- remap ('map','/map') so Nav2/frontier get /map (was the blocker: /map had 0 publishers); (2) rtabmap's internal dense cloud_map/octomap assembly never populated here -> use octomap_server for the 3D on the same /utlidar/cloud_filtered (Grid/3D=false keeps rtabmap LIGHT = 2D+graph only, Orin-friendly); (3) Grid/Sensor=1 (cloud projection); (4) TimerAction delays (rtabmap +14s after EKF, octomap +20s). 16GB Orin RAM. Deliverables: 2D grid /map, 3D octomap /occupied_cells_vis_array+/octomap_point_cloud_centers, pose graph /rtabmap/mapGraph+/mapPath, localization map->odom. rtabmap.rviz created. NEXT: Phase 3 (frontier_explorer on rtabmap /map = 2D frontier autonomy + RTAB 3D), Phase 4 (localization mode). Separate from + doesn't touch the slam_toolbox+frontier backup stack.

**CHECKPOINT 25 (2026-06-25): RTAB-Map Phase 3 blocker = 2D grid has no FREE-space cells.** Phases 1+2 confirmed working (CP24): rtabmap localization (map->odom) + 2D grid + pose graph (128 nodes + LiDAR-proximity loop closures) + octomap 3D (17k voxels) + Nav2 consumes /map. BUT the frontier_explorer (Phase 3) finds 0 frontiers because rtabmap's grid from the 4D-LiDAR cloud (Grid/Sensor=1) is free=0, unknown=2450 -- it marks obstacles but never marks free/ground cells as 0 (frontiers need free-adjacent-to-unknown). Tried subscribe_scan (2D /scan from p2l, Grid/Sensor=0) for clean ray-traced free space -> BROKE rtabmap (0 nodes, no map->odom) with or without rgb. Reverted to the working subscribe_scan_cloud config. Phase 4 (localization mode, localization:=true) ready but not yet tested. DECISION PENDING from user: (A) keep debugging rtabmap cloud-grid free-space, (B) RTAB-Map for localization+grid+3D+Nav2 manual goals only (use slam_toolbox stack for autonomous frontier), (C) hybrid grid. The slam_toolbox+frontier backup stack is untouched + has working autonomous frontier.

**CHECKPOINT 26 (2026-06-25): RTAB-Map Phase 3 SOLVED — frontier runs DIRECTLY on rtabmap /map (Go2-Inspector architecture). The CP25 blocker was a one-line bug.** Cross-checked our stack against the reference repo **github.com/amberhandal/Go2-Inspector** ("Watchdog", ROS2 Kilted, real-Go2). Their frontier explorer subscribes DIRECTLY to rtabmap's 2D grid `/map` (a ~470-line custom C++ node → NavigateToPose, exactly like ours) — they do NOT run it off a Nav2 costmap. The reason THEIR grid has free cells and ours didn't: **`Grid/Sensor`**. RTAB-Map's `Grid/Sensor`: **0 = build the occupancy grid from the SCAN/scan_cloud, 1 = from the DEPTH camera**. We had `Grid/Sensor=1` but `subscribe_depth=false` → rtabmap had NO grid source → `/map` was wall-only/all-unknown (the CP25 "free=0"). (CP25's failed "Grid/Sensor=0" attempt also switched to `subscribe_scan` (2D) which starved rtabmap of the cloud → 0 nodes; the fix is `Grid/Sensor=0` while KEEPING `subscribe_scan_cloud`.) **FIXES (rtabmap_slam.launch.py):** (1) `Grid/Sensor "0"` + `Grid/RayTracing true` + `Grid/NormalsSegmentation false` + `RGBD/CreateOccupancyGrid true` + `Grid/MaxGroundHeight 0.10` + `Grid/RangeMax 5.0` → **/map now has REAL free/occupied/unknown** (FREE 18k at spawn → 65k while exploring). (2) `subscribe_rgb FALSE` (PURE-LIDAR, matches the reference): visual loop closure in the repetitive facility produced a FALSE loop closure that **teleported the pose graph (map→odom jumped to (13.6,-14.6)) → robot drove into a wall and fell (z 0.25→0.057)**. Pure-lidar + `RGBD/ProximityBySpace` (geometric loop closure) → map→odom now stays <0.3 m. The RGB camera is RESERVED for the inspection report (the end goal), not SLAM — same as the reference. (3) `map_empty_ray_tracing FALSE`: our sparse 32-ring lidar + empty-ray-tracing carved phantom free PAST walls → frontiers appeared beyond the facility (goal at x=16, wall at x=15) → robot walked into walls. RayTracing still carves free up to each obstacle (safe). **Nav2:** new `config/nav2_params_rtab.yaml` (passed via new `params_file` arg on nav2.launch.py) — global costmap static_layer=`/map` (frontier + planner share ONE map) + obstacle_layer=`/scan`; **local costmap has NO static_layer** (rtabmap publishes /map sparsely → a local StaticLayer stalls "no map received" → costmap timeout → DWB aborts every goal → robot frozen; /scan obstacle layer gives local obstacles instead). **VERIFIED end-to-end (facility, GUI):** robot autonomously explores on rtabmap's /map — NO falls (z 0.24–0.25 throughout), map→odom <0.3 m, 8+ goals reached, /map FREE 18k→65k, octomap 3D 7.5 Hz, pose graph 473 nodes + proximity loop closures; RViz (rtabmap.rviz, rebuilt) shows /map(=frontier map) + 3D octomap + rtabmap_rviz_plugins/MapGraph pose graph + frontiers + plan in ONE window, alongside the Gazebo GUI. **"rtabmap as a costmap" evaluation → NOT needed: rtabmap's /map IS the map (Grid/Sensor=0); the Nav2-costmap workaround is retired.** RUN: `rtabmap_slam.launch.py world:=facility.sdf headless:=true` → `nav2.launch.py params_file:=.../nav2_params_rtab.yaml` → `frontier_explorer` (default /map) → rviz(rtabmap.rviz)+`gz sim -g`. NEXT (the END GOAL = our use case): SAM3/Claude-vision inspection node → per-run JSON detections (label, map-position, score) → change detection (NEW/MOVED/MISSING/UNCHANGED vs a baseline) → Claude-synthesised inspection report (PDF + annotated 2D map + 3D PLY markers), per the reference's design. 2D slam_toolbox+frontier backup stack still untouched.

**CHECKPOINT 27 (2026-06-25): explored RGB-colour-on-3D-points, then DROPPED it per user — reverted to the clean pure-lidar rtabmap + frontier exploration (CP26 state).** User asked whether the 3D map could be RGB-coloured (camera + lidar) by rtabmap itself. Investigated thoroughly: (1) re-enabled RGB into rtabmap (subscribe_rgb) with STRICT loop closure (Vis/MinInliers 25, RGBD/OptimizeMaxError 1.5) — localization stayed STABLE (no teleport, map->odom <0.3m), proving RGB can be in the SLAM safely; (2) but rtabmap only colours a cloud from DEPTH-per-pixel (RGB-D), which we lack, so synthesized depth from the lidar via rtabmap_util/pointcloud_to_depthimage -> rtabmap RGB-D. **BLOCKERS that made this not worth it:** (a) gz-sim 8.11 `<optical_frame_id>` does NOT relabel the bridged camera_info (stays `camera_link`, x-forward) so the depth projection uses the wrong convention; (b) pointcloud_to_depthimage aborts on TF ("Could not find transform utlidar_lidar->camera_link via odom; extrapolation into the future") -> empty depth (0% valid px); (c) even fixed, colour is at 32-ring lidar density (sparse) -- exported cloud was 569 pts; (d) /rtabmap/cloud_map is the occupancy cloud (uniform red), not RGB. **USER DECISION: drop the RGB-colour requirement, stick to the rtab frontier-exploration approach.** REVERTED rtabmap_slam.launch.py to pure-lidar (subscribe_rgb/depth=false, scan_cloud+odom only, removed the pointcloud_to_depthimage node + rgb/depth remaps, OptimizeMaxError back to 3.0); reverted rtabmap.rviz to the height-coloured octomap 3D (removed the colour-cloud + MapCloud displays). KEPT (harmless, standard, useful for SAM3 later): the `camera_link_optical` frame in robot_gz.xacro + `<optical_frame_id>` in gazebo_gz.xacro; the `go2_perception` pkg with an (unused) `lidar_colorize` node (reserved to host the SAM3 inspection node). VERIFIED clean revert: rtabmap subscribes ONLY /odom + /utlidar/cloud_filtered, frontier explores on /map, robot stable (z~0.25, map->odom <0.12m, 6 goals reached, no falls), octomap 3D 10Hz, RViz+gz GUI up. Net: the colour detour is closed; the deliverable is the stable rtabmap+frontier exploration. NEXT (the END GOAL / our use case): the inspection report (detections + report); RGB camera is used there (2D detection), NOT for 3D colour.

**CHECKPOINT 28 (2026-06-25): FIXED "path hugs walls -> robot clips/falls/reverses into walls" (Nav2 tuning, research-backed).** User reported the planned path runs too close to walls so the robot hits walls/falls or reverses into them; asked to check the frontier algorithm + research repos. **Frontier algorithm audit (frontier_explorer.cpp): SOUND and actually better than the references** -- it has goal blacklisting+TTL, a goal wall-clearance check (hasClearance), picks the cluster cell NEAREST the robot (not the centroid -> avoids centroid-in-wall), info-gain scoring, and uses tf2::TimePointZero. (A research agent confirmed amberhandal/Go2-Inspector's frontier LACKS blacklist + clearance; m-explore/exploration_go2 have them. Reference Nav2 values fetched: Go2-Inspector inflation_radius 0.35-0.55 + cost_scaling_factor 5-10 (10 is a wall-hugger), exploration_go2 factor 3.0; all use BaseObstacle/ObstacleFootprint.scale 0.01-0.02, min_vel_x 0.0.) **ROOT CAUSE = Nav2 costmap, not the frontier:** inflation_radius 0.25 (only ~0.09m beyond the robot's 0.16m inscribed radius) + cost_scaling_factor 3.0 -> the inflated cost collapses just past the wall, so NavFn (which only minimizes cost over the inflated costmap -- it has no separate clearance term) routes along walls; DWB's BaseObstacle.scale 0.02 is too low to pull it off; min_vel_x -0.12 let DWB reverse into the rear LiDAR dead zone; and the collision_monitor is configured but NOT launched (lean nav2.launch.py), so inflation+DWB are the only wall avoidance. The CHAMP gait TIPS when it pushes against a wall (sim artifact) -> the cure is keeping the robot off walls entirely. **FIXES (config/nav2_params_rtab.yaml):** inflation_radius 0.25->0.7 + cost_scaling_factor 3.0->2.5 (BOTH costmaps: a wide gentle gradient -> a cost basin that CENTRES the path; 3m doorways still leave ~1.6m clear); planner use_astar true->false (Dijkstra centres better; A* shortcuts along walls); DWB BaseObstacle.scale 0.02->0.1; DWB min_vel_x -0.12->0.0 (NO controller reverse -> can't back into unseen walls; Nav2's collision-aware BackUp recovery still handles wedges); progress_checker movement_time_allowance 12->20 (min_vel_x=0 makes the robot turn IN PLACE to face goals behind it; 12s tripped the progress checker mid-turn -> false "Failed to make progress"). **Frontier run params:** goal_clearance 0.30->0.6, min_frontier_size 8->10 (goals well off walls + fewer noise frontiers from rtabmap churn). **VERIFIED (fresh spawn, 2 runs 165s + 200s):** path clearance max cost 37/253 with 0/191 path cells near walls (was hugging); ZERO falls (z held 0.24-0.25 throughout); 0 "Failed to make progress"; 6 goals reached, exploring the facility incl. near the south wall (Y=-8) while staying upright. **Test-harness gotchas learned:** (1) `pgrep -fc <pat>` self-matches its OWN command line -> phantom "duplicate" counts (real count = shown - 1); (2) nav2 lifecycle activation is flaky under GUI-startup load (behavior_server change_state timeout) -> launch headless or give it more time, then attach RViz/gz; (3) never test on a robot that fell during a prior edit window -- always clean-restart. RUN: rtabmap_slam.launch.py world:=facility.sdf [headless:=true] -> nav2.launch.py params_file:=.../nav2_params_rtab.yaml -> `frontier_explorer -p use_sim_time:=true -p autostart:=true -p robot_base_frame:=base_link -p goal_clearance:=0.6 -p min_frontier_size:=10` -> rviz(rtabmap.rviz)+gz sim -g. NEXT: optionally bake the frontier params into a launch default + (if ever needed) launch the collision_monitor for a hard safety backstop; then the END GOAL (inspection report).

**CHECKPOINT 29 (2026-06-25): NEXT PHASE planned — Autonomous Zone Sweep + MCP/LLM Gauge Inspection (from `claude_architecture_brief.md`).** New goal on top of the finished mapping stack: robot sweeps a zone, a vision model SEGMENTS+crops analog gauges (no value reading), detections deduped by 3D map position, crops saved per-zone, then Claude (via MCP) reads each gauge → CSV {ID, Type, SI Unit, Reading, Range, Risk}. **Brief-vs-reality gaps found (the brief assumed these done, they are NOT):** (1) "topological zones generated" — we have NO zone subsystem; (2) "camera depth" for dedup — we have mono RGB + LiDAR, NO depth cam; (3) no gauge models in the sim worlds; (4) FastSAM is class-AGNOSTIC (can't ID "gauge" alone); (5) strafe needs vy but our Nav2 has vy=0. **USER DECISIONS (design decisions):** (a) zones = BUILD AUTO zone segmentation (segment /map into rooms); (b) 3D gauge position = PROJECT the detection pixel into the LiDAR cloud (camera_link_optical→…→map; reuses the optical frame we added in CP27); (c) Claude path = Claude Desktop/Code as the MCP client (FastMCP server exposes crops; internet at demo); (d) SIM-FIRST then real (add gauge models to a world; keep nodes sensor-topic-agnostic for the WendyOS port). **VERIFIED ORDER (corrected — brief missed prereqs):** Phase 0 prereqs (gauge sim world + camera→LiDAR 3D-projection utility) → Phase 1 auto zone segmentation (go2_zones) → Phase 2 zone_sweeper (nav center→wall→strafe + FastSAM/YOLO-World + dedup + save) → Phase 3 mcp_gauge_server (FastMCP) + Claude → CSV → Phase 4 integration + real-Go2/WendyOS. **REGRESSION SAFEGUARDS:** every phase is a NEW node/pkg; the RTAB-Map+Nav2+frontier mapping stack is NOT modified; the strafe uses direct /cmd_vel scoped to the sweeper so the frontier's vy=0 Nav2 config is untouched. Research agent running on: FastSAM vs YOLO-World (+Jetson FPS), zone-segmentation algorithm, LiDAR-projection math, gauge-reading prompt. NEXT: fold in research → start Phase 0. Mapping stack zipped for a teammate at ../go2-rtabmap-mapping-stack.zip (CP28 state, untouched).

**CHECKPOINT 30 (2026-06-25): gauge-inspection — research done + final tech decisions locked (user chose brief-faithful, with engineered mitigations).** Research agent findings (evidence-based, cited): (1) **Detection** — FastSAM/SAM2 are class-AGNOSTIC (can't find "gauges"); recommended tiny custom YOLO11n (TensorRT, ~6MB, real-time on Orin Nano) bootstrapped by YOLO-World; public gauge datasets exist. (2) **3D dedup** — camera-LiDAR fusion + wall-plane RANSAC + median depth, store in map frame, message_filters sync + TF at image stamp; sim extrinsic is known from URDF (no calibration); ~5-20cm accuracy -> 0.3-0.5m merge radius. VALIDATES the LiDAR-projection choice. (3) **MCP/Claude** — MCP-returned IMAGES are currently BUGGY in BOTH Claude Desktop and Claude Code; direct Anthropic API is the robust path; offline fallback Qwen2.5-VL:3B/Moondream2 (load on demand). (4) **Gauge reading** — VLMs incl. Claude are UNRELIABLE on analog VALUES (MeasureBench: Claude Opus 4.1 ~15%, best Gemini ~31%; they get unit ~95% but value ~31%); classical CV needle-angle is ~1-2%; hybrid recommended; newer Claude (4.5/4.8) untested. Go2-Inspector correction: it's building CHANGE-DETECTION (SAM3 over HTTP + RealSense depth), NO gauge reading, NO LLM -> gauge reading is net-new; reuse only its sweep->detect->dedup->JSON->report spine. **USER DECISIONS (chose brief-faithful, informed of the risks):** gauge reading = CLAUDE READS EVERYTHING (per brief); transport = MCP SERVER YIELDING IMAGES (per brief); detector = YOLO-WORLD only (open-vocab "gauge"). **ENGINEERED MITIGATIONS (implementation, not architecture changes):** Claude reading -> reasoning-first prompt (scale/unit/needle-vs-ticks then value) + pass known range + square-up-to-gauge (parallax/glare) + confidence field + a clean seam to add CV cross-check later; MCP image bug -> small/compressed crops + file-path fallback + test the round-trip first in Phase 3; YOLO-World -> validate on sim gauges, YOLO11n bootstrap ready as fallback. FINAL ROADMAP unchanged in structure (CP29): Phase 0 (gauge sim world + camera->LiDAR projection util) -> Phase 1 (go2_zones auto segmentation) -> Phase 2 (zone_sweeper: Nav2 center->wall->strafe-via-direct-cmd_vel + YOLO-World + 3D dedup + save) -> Phase 3 (mcp_gauge_server FastMCP + Claude -> CSV) -> Phase 4 (integration + real-Go2/WendyOS). Regression rule holds: all new nodes/pkgs; mapping stack untouched. NEXT: Phase 0 (start with 0b the projection utility + 0a gauge world).

**CHECKPOINT 31 (2026-06-25): gauge-inspection ARCHITECTURE REVISED per user — drop 3D LiDAR-camera projection; dedup via per-zone PANORAMA + post-sweep SAM segmentation.** User is connecting an EXTERNAL camera with UNKNOWN TF, so the camera<->LiDAR 3D projection (CP29/CP30 Phase 0b + the 3D dedup) is REMOVED. NEW perception flow (camera-TF-agnostic): during the zone strafe, COLLECT camera frames -> stitch ONE per-zone PANORAMA of the swept wall (dedup is now SPATIAL: each gauge appears once in the panorama) -> **FastSAM segments the panorama** (post-sweep, one pass) -> crop each gauge along its mask border -> crops -> MCP server -> Claude. This OVERRIDES two just-locked choices: detector YOLO-World(live) -> SAM/FastSAM(on panorama, back to the brief); 3D-LiDAR dedup -> panorama dedup. LiDAR is now used ONLY for SLAM/Nav, not perception. BENEFIT: no camera extrinsic/calibration/tf needed -> the external camera drops in unchanged; pipeline is just an image feed. RISK: dedup quality depends on panorama stitching -> plain low-texture walls can fail/ghost (gauge twice). MITIGATION: stitch using strafe ODOMETRY to place frames (predictable overlap in a controlled sideways sweep) + feature-matching refine + a final position-based merge on the SAM segments as backstop. Note: SAM is class-agnostic -> segments all wall objects; Claude filters non-gauges as it reads (per "Claude reads everything"); optional light size/shape pre-filter knob. REVISED ROADMAP: Phase 0 gauge sim world (0b projection REMOVED) -> Phase 1 go2_zones auto segmentation -> Phase 2 zone_sweeper (Nav2 center->wall->strafe-via-scoped-cmd_vel + COLLECT frames -> per-zone panorama) -> Phase 3 FastSAM segment panorama -> gauge crops -> Phase 4 mcp_gauge_server (FastMCP yields crops) + Claude -> CSV -> Phase 5 integration + real(external cam) + WendyOS. Regression rule holds: all new nodes/pkgs; mapping stack untouched. Supersedes the CP29/CP30 3D-projection plan. NEXT: Phase 0 (gauge sim world) then Phase 1.

**CHECKPOINT 32 (2026-06-25): Phase 0 DONE -- gauge sim world (analog gauges render readably to the camera).** New in go2_worlds: scripts/gen_gauges.py (matplotlib -> 6 analog-gauge PNGs: PRESSURE/VOLTAGE/TEMPERATURE/CURRENT, distinct ranges + needle at a KNOWN reading + red danger arc + gauges_groundtruth.json for scoring Claude) and scripts/gen_gauge_world.py (writes worlds/facility_gauges.sdf = facility.sdf + 6 gauge panels on the SOUTH wall Y=-9.85 z=0.6 X=[-3.5..4]). facility.sdf UNTOUCHED. Added additive optional spawn_yaw arg to go2_champ.launch.py (default 0.0; -> ros_gz create -Y) to face a wall. THREE gz texture gotchas solved: (1) RGBA misread as TRANSPARENT -> flatten to RGB; (2) gz can't resolve texture paths through a SPACE dir (our EE26 Hackathon path) -> no-space HOME symlink ~/.go2_gauge_tex (regenerate per machine); (3) vertical faces get no sun light -> EMISSIVE map. VERIFIED via captured /camera/image_raw: gauges render clearly (ticks/numbers/needle/danger-arc). RUN: ros2 launch go2_bringup go2_champ.launch.py world:=facility_gauges.sdf headless:=true spawn_x:=0 spawn_y:=-8 spawn_yaw:=-1.5708. Mapping stack untouched. NEXT: Phase 1 go2_zones auto zone segmentation.

**CHECKPOINT 33 (2026-06-25): Phase 1 DONE -- go2_zones auto zone (room) segmentation.** New ament_python pkg `go2_zones` with `zone_segmenter.py`: segments an occupancy grid into topological ZONES via distance-transform -> threshold to ROOM CORES (open interiors; narrow corridors/doorways fall below) -> connectedComponents = seeds -> watershed floods each seed across the free space (meeting at walls + mid-doorway) -> one label/room. Each zone -> {id, center, polygon, area} in MAP coords. Runs two ways: ROS node `ros2 run go2_zones zone_segmenter` (subscribes /map OccupancyGrid -> /zones MarkerArray + ~/zones.yaml) OR standalone `python3 zone_segmenter.py map.npz` (offline -> zones.yaml + zones_viz.png). Consumes a STANDARD nav_msgs/OccupancyGrid so it ports unchanged to the live RTAB-Map /map. Also added go2_worlds/scripts/gen_map.py (rasterizes the facility SDF walls -> facility_map.npz, a clean noise-free map for offline algorithm dev; the real pipeline feeds the live /map). **VERIFIED standalone on the facility map: 11 clean zones = 8 rooms + 3 corridor segments, each isolated correctly; the GAUGE room = zone_1 (S-center, center (0,-5.7), 80m2) where all 6 gauges sit.** KEY GOTCHA: do NOT morphologically CLOSE the free mask -- a kernel >= the 0.15m (3-cell) wall thickness bridges the thin walls and fuses all rooms into ONE blob (we hit this: got 1 zone, fixed by removing the close). go2_zones builds clean. Mapping stack untouched. FOLLOW-UP: validate on a real (noisier) rtabmap /map + add a wall-respecting denoise (fill only small fully-enclosed unknown holes) for real maps. NEXT: Phase 2 (zone_sweeper: Nav2 to zone center -> drive to wall -> strafe collecting frames -> per-zone panorama).

**CHECKPOINT 34 (2026-06-25): Phase 1.5 (user request) -- REAL map + rtabmap DB + zones from the explored map.** Per user ("do frontier exploration for 3D mapping first, create rtab db, build zones from the edges of obstacles+walls, then continue"): ran the working mapping stack (rtabmap_slam + nav2 + frontier, the CP28 wall-clear tuning) on facility_gauges.sdf. Robot explored autonomously ~13min, 14 goals, NO falls (z~0.25 throughout), building the live 2D /map + 3D octomap + the rtabmap DB. SAVED: go2-sim/maps/facility_gauges_rtabmap.db (the rtabmap database -- reusable for localization mode / re-running the sweep without re-exploring), go2_zones/maps/facility_gauges_map.npz (the real OccupancyGrid) + facility_gauges_zones.yaml + facility_gauges_zones_viz.png. Ran zone_segmenter on the REAL /map -> **8 zones bounded by the REAL mapped wall/obstacle edges (incl. the gauge panels showing as small obstacles), gauge room = zone_1 center (-0.18,-5.66) 74m2.** This validates the segmenter on a real (noisier) map, not just the synthetic. Coverage note: the run was east-biased -> the far-WEST rooms (X<-5: NW/SW) are not yet mapped (unknown 34k cells); re-running/extending exploration fills them. The segmentation handled the real map's jagged walls cleanly without the synthetic-map's morph-close (which we'd removed). NEXT: Phase 2 (zone_sweeper) -- the sweeper will read facility_gauges_zones.yaml to get the gauge zone's center.

**CHECKPOINT 35 (2026-06-25): Phase 2 DONE (core) -- zone_sweeper: approach wall -> strafe -> per-zone panorama, VERIFIED with all 6 gauges captured each once.** New node `go2_inspection/zone_sweeper.py` (new pkg, mapping stack untouched). FSM: NAV (Nav2 NavigateToPose to the zone center, facing the gauge wall) -> APPROACH (scoped /cmd_vel straight at the wall until ~approach_dist off, via /scan front beam) -> STRAFE_START (strafe to -half_width) -> SWEEP (strafe to +half_width, grabbing a camera frame every frame_interval of lateral travel, with a small vx correction holding wall distance) -> STITCH (place each frame's central strip at its lateral position -> panorama.png + panorama_meta.json) -> DONE. **FRAME-INDEPENDENT design (key):** lateral position = robot displacement projected onto the LATERAL axis recorded at the approach point (lat_axis = [cos(yaw+pi/2), sin(yaw+pi/2)] from the tf map->base_link yaw), NOT absolute map-X -> works in ANY map orientation and in SLAM OR localization mode. **Camera-frame + odometry only -> NO camera extrinsic/TF needed** (ports unchanged to the external real camera per CP31). Nav2 owns macro-nav ONLY; approach+strafe use DIRECT /cmd_vel (the Go2 gait accepts lateral vy) SCOPED to this node, so the frontier's vy=0 Nav2 config is never touched. Stitch is ODOMETRY-based (strip placement, ppm=fx/wall_dist) -> robust on plain low-texture walls (no feature matching). **VERIFIED (skip_nav core test):** robot spawned in the gauge room at (0,-8) facing south (spawn_yaw=-1.5708) in SLAM mode on facility_gauges.sdf; sweeper APPROACHed to ~1 m off the south wall (Y -8 -> -8.9), strafed -4.3 -> +4.3 m collecting frames, STITCHed a **3322x480 panorama containing ALL 6 gauges, EACH APPEARING EXACTLY ONCE** (PRESSURE/VOLTAGE/TEMPERATURE/bar/CURRENT/PRESSURE, left->right) -> spatial dedup CONFIRMED. Robot upright throughout (z 0.24-0.25, no falls). Output: ~/gauges/zone_1/panorama.png + panorama_meta.json {zone_id, wall_dist, ppm, lat_range, n_frames}. Added additive spawn passthrough (spawn_x/y/yaw) to rtabmap_slam.launch.py. **GOTCHA:** `use_sim_time` is auto-declared in Jazzy -> do NOT self.declare_parameter it (crashed ParameterAlreadyDeclared; fixed -- pass it via -p use_sim_time:=true). **REFINEMENT (revisit if Phase 3 FastSAM struggles):** the strip stitch shows mild vertical-strip shearing (wall_dist/strafe-speed variance vs a fixed ppm) -> gauges are readable + cleanly separable but slightly sheared; options = per-frame wall_dist scale, or strip blending. **FOLLOW-UP (deferred, does not block the core):** macro-nav in localization mode -- rtabmap localization publishes only the grid AROUND the relocalized area (partial /map), so Nav2 can't path to a far zone center; fix = static map_server from the saved grid for nav, or run nav in SLAM mode. The skip_nav path validates approach+strafe+stitch independently. RUN: `ros2 launch go2_bringup rtabmap_slam.launch.py world:=facility_gauges.sdf headless:=true spawn_x:=0 spawn_y:=-8 spawn_yaw:=-1.5708` then `ros2 run go2_inspection zone_sweeper --ros-args -p use_sim_time:=true -p skip_nav:=true -p zone_id:=zone_1 -p half_width:=4.3`. Mapping stack untouched. NEXT: Phase 3 (FastSAM segments the panorama -> crop each gauge along its mask -> ~/gauges/zone_N/gauge_*.png).

**CHECKPOINT 36 (2026-06-25): Phase 3 DONE -- FastSAM panorama gauge segmentation -> clean per-gauge crops (all 6, each once, readable).** New module `go2_inspection/panorama_segmenter.py` (console_script `panorama_segmenter`; new dep on `ultralytics` FastSAM, torch+CUDA). PIPELINE: FastSAM segments the per-zone PANORAMA (class-agnostic) -> filter gauge-like blobs (bright face vs the dark wall, 40-340px, aspect 0.4-2.6, off the floor) -> MERGE by horizontal-interval overlap (collapses FastSAM's over-segmented rim/dial/danger-arc fragments AND cross-scale duplicates into one detection per gauge; distinct gauges are far apart in x so they stay separate) -> one detection/gauge with its LATERAL position (panorama col -> lat via ppm/lmin). **KEY INSIGHT: the strip-stitched panorama SHEARS the needle (= the value!), so it is used ONLY for detection + spatial dedup; the actual crop Claude reads is taken from the SINGLE source frame where the gauge sits centred (un-sheared, full-res, nearest captured lateral position) -- this is why Phase 2/CP35 also saves the 126 frames + frames.json.** Output: ~/gauges/zone_1/gauges/gauge_NN.png + gauges.json ({id, zone, lateral, src_frame, bbox_pano, bbox_frame}) + gauges_contact_sheet.png. **VERIFIED:** all 6 gauges found at lat -3.57/-2.04/-0.55/+0.99/+2.47/+3.99 m (= the placed X=[-3.5,-2.0,-0.5,1.0,2.5,4.0]); the best-frame crops are CLEAN + readable -- PRESSURE/psi, VOLTAGE/V, TEMPERATURE/degC, PRESSURE/bar (danger arc), CURRENT/A (needle in the red danger zone), PRESSURE/psi (danger arc); scale+unit+needle+danger-arc all clear. **GOTCHA (detection robustness): FastSAM-s is resolution-SENSITIVE + NON-monotonic** -- whole-image @1024 found 5 (missed the +2.5 gauge), @2048 found 6, @3072 found only 2, and 1000px-tiling @1024 found 3. SOLUTION = **multi-scale UNION** (run whole-panorama FastSAM at scales [1024,2048], POOL all boxes, then interval-merge) -> robust 6/6 without betting on a single magic imgsz; tunable via `--scales`. **REAL-TIME:** the 2 FastSAM passes run ONCE post-sweep, OFF the locomotion control loop, so they never block the robot; FastSAM-s (~23MB) is Orin-Nano-friendly (a few hundred ms/pass). Mapping stack untouched; all-additive (Phase 2 frame-saving + new module). NEXT: Phase 4 (mcp_gauge_server: FastMCP yields the crops -> Claude reasoning-first read -> CSV {ID, Type, SI Unit, Reading, Range, Risk, Confidence}; score against go2_worlds gauges_groundtruth.json).

**CHECKPOINT 37 (2026-06-25): Phase 4 DONE -- Claude reasoning-first gauge reading -> inspection CSV; full pipeline scores 6/6 vs ground truth.** Two new standalone modules in go2_inspection (NOT ROS console_scripts -- they need anthropic/fastmcp, which live in a venv `~/gauge_venv`, separate from system-python ROS; ultralytics for Phase 3 IS in system python). (1) **gauge_inspector.py** -- the robust ON-DEVICE/real-time path: one Anthropic API call per crop (claude-opus-4-8, post-sweep, off the locomotion loop) with a **REASONING-FIRST prompt** (label->unit->range->needle-vs-ticks->interpolate->risk-vs-red-danger-arc->confidence) and a forced `report_gauge` tool for structured output; aggregates -> inspection_report.csv {ID, Zone, Type, Reading, Unit, SI_Unit, Range, Risk, Confidence} + readings.json; scores vs ground truth (matched by left->right lateral order). Reasoning-first + clean full crops are the mitigation for the known VLM analog-value weakness (CP30: ~15-31% one-shot). NO ground truth is given to the model -- it reads the range off the dial -> ports to the real robot. (2) **mcp_gauge_server.py** -- the brief-faithful transport: a FastMCP server exposing `list_inspection_zones / get_zone_gauges (yields the crops as images + the reading instructions) / get_zone_gauge_paths (the MCP-image-bug FILE-PATH fallback per CP30) / get_gauge_image / save_inspection_report` for Claude Desktop/Code. Both share the same reasoning-first instructions. **VERIFIED:** (a) MCP transport smoke-tested in-memory (fastmcp 3.4.2 Client): 5 tools, get_zone_gauges returns **6 images**, path-fallback returns 6 paths, save_inspection_report writes the CSV. (b) END-TO-END reading: Claude read the 6 Phase-3 crops (reasoning-first) -> through the real rows_to_csv+score code -> **type 6/6, unit 6/6, value 6/6 (0.0% error)** vs gauges_groundtruth.json (PRESSURE/psi 45, VOLTAGE/V 220, TEMPERATURE/degC 90, PRESSURE/bar 7 WARNING, CURRENT/A 30, PRESSURE/psi 175 WARNING). NOTE: this validation reading was performed by Claude THIS SESSION on the actual crops (the exact step gauge_inspector automates via the API and mcp_gauge_server exposes to Claude Desktop) -- the deploy demo needs ANTHROPIC_API_KEY or a Claude Desktop MCP hookup; no key was present here, so the API call itself was not exercised (structurally complete + import-clean). **CROP-QUALITY FIX (this session, in panorama_segmenter):** the first crops CLIPPED the dim side of each dial (a high needle, e.g. the 220 V, fell just outside the FastSAM bbox). Fixed: the panorama bbox is used ONLY to pick the gauge + its best frame; the crop is then RE-CENTRED on the bright gauge PANEL re-detected in the clean frame (`crop_full_gauge`) -> the whole dial+needle is always inside. Re-verified all 6 crops show the full scale+needle+danger-arc. Run docs: go2_inspection/README.md (the two-runtime split + the full sim command sequence). Real-time-safe: Phase 4 is post-sweep, off-loop ([[realtime-on-device-constraint]]). Mapping stack untouched; all-additive. NEXT: Phase 5 (integrate the 4 phases into one orchestrated per-zone run + multi-zone patrol; then the real Go2 external camera + WendyOS containerized deploy).

**CHECKPOINT 38 (2026-06-25): ENVIRONMENT REDESIGN -- realistic multi-room inspection facility (gauges distributed across 5 rooms); future phases DOCUMENTED not implemented (per user).** Per user: the 6 gauges were all clustered on the SC south wall (one room) -> redesign so assets are DISTRIBUTED across the facility and the robot must genuinely navigate room-to-room. NEW additive generator `go2_worlds/scripts/gen_inspection_world.py` (reuses gen_gauges.draw_gauge; facility.sdf, facility_gauges.sdf, gauge_tex/, gen_gauges.py, gen_gauge_world.py ALL UNTOUCHED) -> writes **facility_inspection.sdf = facility.sdf + 12 assets across 5 rooms** (NW/NC/NE/SC/SW off the central corridor), **5 asset types** (PRESSURE/VOLTAGE/TEMPERATURE/CURRENT/FLOW), with **3 seeded ANOMALIES** reading into the red danger zone (NC-TEMP-2 overheat 185/200, NE-VOLT-2 over-voltage 470/500, SW-FLOW-1 over-flow 480/500) so the future anomaly/fault detection has real faults to find. Panels face INTO their room via per-wall yaw (south/north=0, east/west=pi/2). Separate `inspection_tex/` + `~/.go2_inspection_tex` no-space symlink + `inspection_groundtruth.json` (id, asset, room, wall, type, unit, range, true_reading, anomaly, pose). **VERIFIED:** `gz sdf -k` = Valid; rebuilt go2_worlds (world in install); spawned the robot in the NE room facing east + captured /camera/image_raw -> the **yaw-rotated east-wall VOLTAGE gauge renders correctly facing the room, needle in the red zone** (the over-voltage anomaly visible) -> the new rotated-panel placement works; south-wall rooms (SC/SW) reuse the proven yaw=0 facility_gauges approach. HOME (robot start for every mission) = (0,0) corridor, facing +X. **FUTURE PHASES DOCUMENTED ONLY (per user: do NOT implement until told)** in new `docs/03-INSPECTION-ROADMAP.md`: the autonomous MISSION spec (HOME->localize->autonomously navigate->inspect->read->detect anomalies->report, **NO CHEATS** = only the robot's own sensors/SLAM/localization/planning; forbidden: GT poses, teleport, SDF-maps-for-nav, hardcoded waypoints, mission-time skip_nav), Phase 5 (mission orchestration: one command end-to-end + per-phase standalone; PREREQS = 5a complete re-explore/map of the NEW world since the current DB is facility_gauges + east-biased, 5b localization-mode partial-/map nav fix via static map_server), Phase 6 (anomaly detection, score vs the seeded flags), Phase 7 (real Go2 external cam + WendyOS), Phase 8 stretch (NL missions). DEFERRED + recorded (not lost): the repo restructure + `legacy/` archive + the full professional README (paused to keep changes incremental/non-breaking per user; resume on request). All-additive; the existing facility_gauges Phase 2-4 pipeline is untouched and still works. NEXT: await explicit go-ahead to implement Phase 5.

**CHECKPOINT 39 (2026-06-25): Phase 5a DONE -- complete AUTONOMOUS re-map of facility_inspection (all 6 rooms) -> DB + grid + 13 zones. (Hard-won; key learnings below.)** User approved re-mapping the new world. Final result: fresh autonomous frontier exploration (LiDAR -> rtabmap SLAM -> /map -> frontier_explorer -> Nav2 -> drive) mapped ALL 6 rooms in ~20 min (center -> west -> east), robot upright throughout. NO CHEATS: pure LiDAR SLAM + autonomous frontier + Nav2 planning; the only sim signal is the gz odometry plugin = real-Go2-EKF mirror (CP13). SAVED to go2-sim/maps/: `facility_inspection_rtabmap.db` (382M), `facility_inspection_map.npz` (full-facility grid, 220k free cells, all rooms 86-95%), `facility_inspection_zones.yaml` (13 zones) + `_zones_viz.png`. **Gauge-room -> zone map: NE=zone_0, SW=zone_1, SC=zone_2, NC=zone_3, NW=zone_10** (all 5 gauge rooms covered). **LEARNINGS / GOTCHAS (critical for the real deploy + reruns):** (1) **DDS shm corruption** -- after the user's IDE crashed mid-run + many restarts, ~154 stale FastRTPS shared-memory lock files piled up in /dev/shm -> `open_and_lock_file failed fastrtps_portNNNN` -> degraded ROS comms -> **the robot could not be commanded** (Nav2 goals/cmd_vel silently not flowing; looked like a nav bug, wasn't). FIX = **`export FASTDDS_BUILTIN_TRANSPORTS=UDPv4`** (disable Fast-DDS shared-memory transport -> no lock files; UDP handles the lidar/map fine on localhost). STANDING RULE: set this env for every sim/ROS run on this laptop; also `rm -f /dev/shm/fastrtps_* /dev/shm/sem.fastrtps_*` after a crash. (2) **rtabmap occupancy grid is LOCAL-only** -- loading a saved DB (localization OR new `continue_map` mode, even with Mem/InitWMWithAllNodes=true -> WM=2502 nodes) only republishes the ~180x218 grid AROUND the robot; `publish_map` service (field is `global_map` not `global`) republishes the GRAPH but not a global grid; `rtabmap-export` is 3D-only (no 2D grid). => you CANNOT extract a full grid from a static DB; the full grid only materializes as the robot physically TRAVERSES. So the grid must be saved LIVE during a mapping run (now done every check via map_grab). (3) **Directed-waypoint patrol FAILS on a fresh/growing map** -- the global planner can't path to far goals sitting in unknown space ("GridBased failed to plan"); only the frontier's boundary-goals work for autonomous mapping (also keeps it cheat-free). (4) **Frontier east-crossing** -- the first run's distance penalty (potential_scale 3.0) trapped it in the west (never crossed to the far east); the successful run used default tuning but simply ran long enough to branch center->west->east. ADDED (additive) `continue_map:=true` to rtabmap_slam.launch.py (resume + extend a saved ~/.ros/rtabmap.db, no -d) -- useful for the real robot (don't re-map from scratch) even though it didn't solve the grid-extraction. The partial west+center DB from the crashed run is preserved as a backup. The facility_gauges map/DB/pipeline are untouched. NEXT: Phase 5b (static map_server serving facility_inspection_map.npz for Nav2's global costmap + rtabmap localization for map->odom -> robust facility-wide nav), then 5c (mission orchestrator).

**CHECKPOINT 40 (2026-06-25): Phase 5b DONE -- facility-wide autonomous navigation (solves the CP35 localization-nav blocker).** The mission needs Nav2 to plan across the WHOLE facility, but rtabmap's loc-mode /map is only a local window (CP39). SOLUTION: split the map from the localizer. (1) `maps/npz_to_map.py` converts the saved grid -> standard `facility_inspection_map.pgm`+`.yaml` (620x428). (2) New launch `go2_bringup/launch/inspection_nav.launch.py`: rtabmap in LOCALIZATION mode provides map->odom (its grid remapped OFF /map to /rtabmap/grid_map via the new additive `grid_topic` arg on rtabmap_slam.launch.py), a static `nav2_map_server` serves the FULL grid on /map, Nav2 (nav2_params_rtab) plans on it. No-space `~/.go2_maps` symlink for the map path. **VERIFIED:** launched headless, robot localized at HOME (map->base_link=0,0), /map=620x428 full static map, Nav2 active, shm=0 (UDPv4). Sent a NavigateToPose goal to the far NE room (10.19,6.01) -> the robot autonomously drove HOME(0,0) -> east corridor -> through the NE doorway -> arrived (9.66,6.19), upright. Facility-wide nav works. NO CHEATS (rtabmap localization + Nav2 planning; HOME start is the known dock). All-additive: grid_topic arg + inspection_nav.launch.py + npz_to_map.py are new; existing launches untouched. NEXT: Phase 5c (mission orchestrator: for each gauge zone, Nav2 to a wall-approach pose -> zone_sweeper -> panorama_segmenter -> gauge_inspector -> one facility report -> return HOME; one command + per-phase standalone).

**CHECKPOINT 41 (2026-06-26): Phase 5c -- mission orchestrator + one-command launch; end-to-end autonomous loop VALIDATED (single room).** New `go2_inspection/inspection_mission.py` (console_script `inspection_mission`): from HOME, for each gauge room it Nav2's to a wall-APPROACH pose (facing the wall) -> runs `zone_sweeper` (skip_nav; scoped /cmd_vel approach+strafe -> panorama+frames) -> `panorama_segmenter` (FastSAM crops) -> optional `gauge_inspector` (Claude read, only if ANTHROPIC_API_KEY) -> returns HOME -> writes `~/gauges/facility_inspection_manifest.json`. Sweeper/segmenter/inspector run as SUBPROCESSES (clean rclpy isolation); a `zones:=` param filters rooms for testing. Per-room MISSION config (approach pose + strafe half_width) derived from the CP38 layout: NC(north wall), NE(east), SC(south), SW(south), NW(west). New one-command launch `go2_bringup/launch/mission.launch.py` = inspection_nav (5b) + the orchestrator (starts after localization+Nav2 up). Additive fix: `zone_sweeper` now EXITS when it reaches DONE (run-once tool, so the orchestrator can sequence it). **VALIDATED end-to-end on NE (zones:=NE) against the running 5b stack: NAV->approach NE (13.3,6.0) -> ARRIVED -> sweep -> segment -> 2 gauges -> return HOME (0.31,0.15) -> manifest written.** Full autonomous nav->sweep->segment->report->home loop, one command. NO CHEATS (Nav2 on the robot's localized map; camera+lidar only). **REFINEMENTS (honest, open):** (a) the FULL 5-room run has not yet been executed end-to-end -- the multi-room logic is there + validated on ONE room; (b) mission sweeps are skewed by Nav2's arrival-YAW tolerance (vs the Phase-2 precise hand-spawn) -> more panorama shear -> NE segmented 2 of its 3 gauges (all 3 ARE in the panorama; not a scale issue). FIX = "square-up-to-wall" in the sweeper's APPROACH (rotate to the wall normal via /scan before recording the lateral axis) so the strafe is perpendicular regardless of arrival yaw. All-additive; Phases 2-4 + facility_gauges untouched. NEXT: square-up refinement + the full 5-room mission run; then Phase 6+ (awaiting go-ahead): anomaly detection, real Go2/WendyOS, NL missions, repo restructure + README.

**CHECKPOINT 42 (2026-06-26): two fixes during the user's hands-on self-built RTAB mapping run -- ground/floor in the costmap+voxels, and a sideways-drifting robot.** While the user ran the Section-F manual mapping (T1 rtabmap_slam + T2 rviz + T3 nav2 + T4 frontier) they saw (a) the floor voxelised + feared it polluting the costmap, and (b) the robot strafing sideways at launch. ROOT CAUSES + FIXES (both verified against the live process tree): **(a) GROUND.** The L1 is mounted pitched ~8.6 deg DOWN (`utlidar_joint` rpy `0 0.15 0`, robot_gz.xacro) so its rings rake the floor a few m out, and base_link rides at the gait `nominal_height` ~**0.225m** (the pointcloud_to_laserscan comment's "~0.40" was stale). So the floor sat at ~base_link z=-0.225 and the old `/scan` `min_height: -0.20` left only ~2.5cm margin -> the floor LEAKED into /scan on every gait bob -> phantom ground obstacles in BOTH Nav2 costmaps' obstacle_layer. Fix 1: `pointcloud_to_laserscan.yaml min_height -0.20 -> -0.14` (~8cm floor-rejection margin incl. bob; still sees every real obstacle -- walls 1.6m, tables 0.7-0.9m; blind only to sub-0.08m clutter, none here). The separate blue voxel-blanket in RViz is the OCTOMAP (`/occupied_cells_vis_array`), which is NOT a costmap source (cosmetic), driven by `octomap.launch.py pointcloud_min_z: -0.2`; Fix 2: `-0.2 -> 0.08` removes the floor from the 3D viz, walls remain. The SAVED rtabmap 2D grid was always floor-clean (`Grid/MaxGroundHeight 0.10`), so any map built before this is still good. Both files symlink-installed -> edits live with NO rebuild, but a RUNNING octomap/p2l node keeps old params until the stack is relaunched (so the fix shows on the NEXT launch). **(b) SIDEWAYS.** The process tree had TWO ~1.5h-old ZOMBIE `zone_sweeper` procs (NE half_width 2.6, SC half_width 3.2) from an earlier inspection test, still alive publishing `/cmd_vel` `linear.y` (the strafe). Launched when no sim was up, they sat idle then resumed the instant the fresh sim brought up /odom+/scan -> drove the live robot sideways. Killed both by exact PID (current mapping stack -- launch 212290, gz, champ, rtabmap 213055, rviz, + the user's T3 nav2 + T4 frontier -- left untouched) + sent one zero `/cmd_vel` (CHAMP has no cmd_vel watchdog in sim -> holds the last command). Nav2 DWB `max_vel_y=0` -> the planner never strafes, so sideways == the sweeper, never nav. No code beyond the two param tweaks; mapping/perception logic untouched. Docs: RUN-SIM.md notes + [[rtabmap-frontier-config]] updated with both gotchas. **(c) FOOTPRINT (per user "use the actual Go2 standard footprint"):** both Nav2 costmaps' footprint `[[0.26,0.16]...]` (0.52x0.32, which CLIPPED the front L1/camera head at x=+0.30) -> `[[0.35,0.155]...]` = **0.70x0.31 = the Unitree Go2 standard standing footprint** (from the URDF: trunk 0.376m + head sensors at x=0.30 + hips x=±0.19 + feet y≈±0.14). Width 0.31 stays the aisle-limiting dim (corridor 3m, doorways >=1.5m -> turn-in-place circle 0.76m fits), so low regression risk; more accurate + robust for the real robot. nav2_params_rtab.yaml is symlink-installed -> live on next nav2 launch.

**CHECKPOINT 43 (2026-06-26): SERVICE TRIGGER-LAYER -- the multi-terminal stack wrapped as 12 ROS2 services (the future MCP tool surface). NO MCP/Wendy yet, per user.** User: "the stack is too complex to run ... create service based trigger commands ... I should be able to call a service to start frontier navigation ... zone approach / total navigation for extracting gauge information." Built a thin **subprocess-orchestrator** service node (same proven pattern inspection_mission already uses) so the existing validated nodes are reused as children -- the working nodes are UNTOUCHED, no rclpy single-context/threading risk. NEW: (1) `go2_inspection_interfaces` (ament_cmake) with ONE uniform srv **ZoneTask** `{string zone_id, bool read} -> {bool success, string message, string result_json}` -- uniform shape so the future MCP layer wraps every tool with one client pattern. (2) `go2_inspection/mission_control_server.py` (console_script `mission_control_server`) advertising **12 services**: MAPPING `/start_exploration /stop_exploration /save_map`; NAVIGATION `/navigate_to_zone /navigate_home`; INSPECTION `/inspect_zone /run_mission`; DATA `/list_zones /get_zone_image /get_zone_gauges /get_report /get_status`. Holds a frontier child handle + a robot-busy lock (one motion at a time) + a cached /map (status/coverage) + MultiThreadedExecutor (status/stop stay live during a long inspect). Each heavy service = subprocess of the proven node: navigate/inspect/run_mission -> `inspection_mission` with new flags; save_map -> map_grab->zone_segmenter->npz_to_map->cp db; start_exploration -> `frontier_explorer` (SIGINT to stop). (3) `inspection_mission.py` gained ADDITIVE flags (defaults preserve old behaviour): `inspect` (False=nav only), `return_home`, `goto_home` (just dock HOME), `read` (AND-gated on ANTHROPIC_API_KEY) -- so one node composes navigate-only / single-zone / dock. (4) `go2_bringup/launch/mission_control.launch.py` (server only; additive, does NOT bring up the sim -- run beside whatever base stack is up: SLAM+Nav2 for mapping, localization+map_server+Nav2 for inspection). **VERIFIED:** colcon build clean (interfaces+go2_inspection+go2_bringup); server starts ("mission_control up: 12 services"); data services respond WITHOUT the sim -- /list_zones -> 13 zones JSON, /get_status -> frontier=False busy=idle known=0%, /get_report -> reads the existing manifest; launch parses + symlinked. Robot-motion services (nav/inspect/mission/explore) are wired to the proven inspection_mission/frontier subprocess paths (need the live stack to exercise). Docs: `docs/04-SERVICE-LAYER.md` (catalog + examples + the 2 base-stack modes + the 1:1 MCP mapping). All-additive; zero changes to the working perception/nav logic. **DEFERRED (explicit, next):** the WendyOS MCP server exposing these 12 services as MCP tools (e.g. get_zone_image returning real image bytes). Mode note: start the right base stack first (mapping vs inspection); the server doesn't switch stacks.

**CHECKPOINT 44 (2026-06-26): STACK AUDIT (8-agent adversarial workflow) -> 2 real bugs fixed + 2 non-bugs clarified.** User reported 3 symptoms after running the maze/service stack; ran a multi-agent investigate->adversarial-verify audit of 4 concerns. Verified outcomes + fixes (all additive; validated facility sweep+mapping untouched): **(1) STRAFE COLLISION (REAL BUG, fixed).** zone_sweeper's STRAFE_START/SWEEP strafe was OPEN-LOOP /cmd_vel linear.y with ZERO lateral collision check (`_scan` only cached the FRONT median for vx-hold); a too-large `_derive_half_width` (polygon spans past a perpendicular wall) or a wrong FIND_WALL square-up drove it sideways into a wall with nothing to stop it. FIX: new `side_clear` param (0.45m) + `_lateral_clear(sign)` helper (min /scan range in a +-20deg arc centred on the strafe direction: SWEEP +vy->+90deg left, STRAFE_START -vy->-90deg right, reusing the cached self.scan) gating BOTH strafe states -> HALT before contact (SWEEP ends gracefully into STITCH with frames-so-far). The audit also corrected a wrong rationale: the collision_monitor is DEAD CONFIG (go2_bringup/nav2.launch.py never launches it), so /cmd_vel isn't "bypassing safety" -- there simply is none in that loop; added a clarifying note to nav2_params_rtab.yaml + fixed the misleading "scoped /cmd_vel" docstring (it's the GLOBAL /cmd_vel, safe only because Nav2's controller is idle during the sweep). **(2) BUSY-LOCK LEAK (REAL BUG, fixed).** mission_control_server: start_exploration acquires self._busy and it's released ONLY in stop_exploration; if the frontier child exits on its own (exploration COMPLETE or crash), the lock stayed held + _action='exploration' -> every navigate/inspect/run_mission returned "robot busy" forever (recoverable only via /stop). FIX: `_frontier_running()` now self-reaps -- on a dead child it nulls the handle, calls _release(), warns, returns False -> motion services auto-recover, get_status reads idle. Idempotent + additive (live child path unchanged). **(3) /get_status "stale" (NOT a bug).** Verified the /map cache updates live (sub QoS proven compatible -- frontier_explorer consumes the identical QoS and explored) and 79.9% is the CORRECT converged coverage: the grid bbox keeps ~9.5k unknown cells beyond the walls, so known% plateaus <100% even when COMPLETE; identical before/after stop = the map had already converged. Clarity-only: get_status now also reports `map_unknown_cells`. **(4) MODE 2 map "in another location" (NOT a code bug; refuted the first hypothesis).** npz_to_map origin/flip is provably correct (yaml origin == npz origin; +h*res would inject ~21m); the DB graph-origin == HOME == (0,0) and the robot respawns at (0,0), so map==odom==graph-origin coincide (the proposed RGBD/StartAtOrigin "fix" would WRONGLY mis-anchor non-HOME-spawn missions like facility_gauges@(0,-8) -> NOT applied). Real cause is operational: wrong localization DB (cp <map>.db -> ~/.ros/rtabmap.db) or RViz showing /rtabmap/grid_map (local window) vs the authoritative static /map. Added a tf2_echo diagnostic + the DB/RViz guidance to RUN-SIM.md (no code change). VERIFIED: py_compile clean (zone_sweeper + server); server boots + advertises 12 services; get_status shows the new unknown field. Docs: RUN-SIM.md troubleshooting (strafe gate, MODE2 offset, known% plateau). Workflow: 8 agents, 253k tokens, 4 concerns investigate->verify.

**CHECKPOINT 45 (2026-06-26): MODE-2 localization ROTATION fix + frontier short-goal stepping & premature-stop fix; adversarially reviewed (3-lens workflow) -> 1 self-introduced livelock caught & fixed.** User screenshot showed the MODE-2 static map vs the live rtabmap-localized frame ROTATED apart, + frontier exploration stopping prematurely; asked to cap frontier goal distance (short goals, re-eval). Fixes: **(A) ROTATION (rtabmap_slam.launch.py).** rtabmap localization was doing a GLOBAL relocalization at startup and snapping to a ROTATED match in the near-symmetric maze. Added `loc_params["RGBD/StartAtOrigin"]="true"` (empirically confirmed the param exists in librtabmap_core 0.22) -- the robot ALWAYS respawns at HOME=map origin, so assume start-at-origin instead of global reloc. Scoped to loc_params ONLY (map_params/cont_params untouched -> SLAM/continue mapping unaffected; verified by the regression reviewer). Real-robot caveat (documented): StartAtOrigin bakes in any hand-placement offset (no reloc corrects it) -> mark a HOME spot / publish /initialpose on hardware. **(B) FRONTIER short-goal stepping (frontier_explorer.cpp, used in BOTH maze + facility mapping).** Root cause of premature stop: far goals timed out/wedged -> Nav2 failed -> frontier BLACKLISTED -> far regions abandoned -> "no reachable frontiers -- COMPLETE". Added: `max_goal_distance` (3.0) + `clampGoal`/`cellNavigable` -> if the best frontier is >3m, snap the goal to the farthest navigable+non-blacklisted cell along the robot->frontier ray (short reachable STEP), re-eval on arrival. Termination no longer quits on first empty: if frontier CELLS remain but all blacklisted, clear the blacklist + retry (bounded by `max_clear_retries`=3), require `done_confirm`=3 consecutive empty cycles. **ADVERSARIAL REVIEW (3 lenses: correctness/livelock, regression, edge/real-robot) caught a MUST-FIX I introduced:** resetting the clear-retry budget on goal NEAR-ARRIVAL was wrong -- near-arrival fires when Nav2 did NOT complete (circling a frontier it can't consume), so it defeated max_clear_retries -> LIVELOCK (circle->blacklist->clear->circle forever). FIXED: gate the `clears_done_` reset on actual MAP GROWTH (new-free-cell high-water mark), not on arrival/SUCCEEDED -> the DONE bound is robust to circling. Also (review-driven): prune TTL-expired blacklist entries each tick (clampGoal scans the vector ~44x/tick -> keep it bounded); degenerate-config guard in clampGoal (res<=0 / max<min). KNOWN/acceptable (reviewer-confirmed, not blocking): max_goal_distance=3.0 also clamps the 30x20 facility -> slower but NO coverage gaps (raise via `-p max_goal_distance:=6` if needed); clamped-intermediate blacklisting can shadow a frontier just-beyond-cap for <=TTL (benign, area still explored). VERIFIED: colcon build go2_exploration clean (x3 across fixes). All changes additive; the validated facility mapping + facility_gauges sweep untouched. Docs: RUN-SIM.md troubleshooting (rotation, short-goal stepping, real-robot caveats). Review: 3 agents, ~98k tokens.

**CHECKPOINT 46 (2026-06-26): enabled OMNIDIRECTIONAL (lateral vy) in the DWB controller -- the one legged-specific Nav2 win (user-chosen, real-robot-minded).** User asked whether DWB+NavFn is optimal for legged robots + to apply standard params. RESEARCH FINDING (important): DWB+NavFn IS the standard for CHAMP quadrupeds -- the official `rtabmap_demos/params/champ_nav2_params.yaml` uses exactly `dwb_core::DWBLocalPlanner` + `nav2_navfn_planner` with `max_vel_y:0`, and the community Go2 repos (arjun-sadananda/go2_nav2_ros2, anujjain-dev/unitree-go2-ros2) run CHAMP + Nav2 DifferentialMotionModel. Our velocity limits already match the Go2 gait (max_vel_x 0.26<gait 0.30; max_vel_theta 0.4=gait 0.4) -- better-matched than the generic champ file (which over-specs wz=1.0). MPPI is the modern legged-capable successor (Nav2 docs) BUT compute-heavy -> a real-8GB-Orin risk (and we reverted from it before), so DWB stays the right Orin-friendly choice. The genuine legged win the user picked: ENABLE OMNI so DWB can use the Go2's strafe (the Go2 is 0.31m WIDE vs 0.70m LONG -> fits sideways through gaps it can't enter nose-first). nav2_params_rtab.yaml DWB `FollowPath`: `min_vel_y 0->-0.15`, `max_vel_y 0->0.15` (= the validated sweep strafe speed, conservative/real-robot-safe; < gait 0.25), `acc_lim_y/decel_lim_y 0->+-0.6`, `vy_samples 0->5`; `max_speed_xy` stays 0.26 (total speed bounded -> trades forward for lateral); added `min_y_velocity_threshold 0.001`; updated the (dead) velocity_smoother y-limits for consistency. Critics + wall-clearance tuning UNCHANGED -> the robot still prefers forward/centered (PathAlign/GoalAlign dominate) and only strafes for fine moves. nav2_params is symlink-installed -> live on next nav2 launch, no rebuild. YAML validated. **TEST in sim first** (frontier explore / a nav goal): confirm it sidesteps cleanly without lateral oscillation; if it dithers, drop `vy_samples` to 3 or `max_vel_y` to 0.10, or REVERT to vy=0 (= the proven CHAMP-standard behavior). Real-robot: Go2 sport mode / go2-rc `/velocity{vx,vy,vyaw}` accepts lateral, so this ports; keep vy<=0.15 for stability on first hardware runs. Additive; frontier/sweeper/mapping logic untouched (sweeper still strafes via its own direct /cmd_vel). Sources: rtabmap_demos champ_nav2_params.yaml, docs.nav2.org select_algorithm + configuring-mppic.

**CHECKPOINT 47 (2026-06-26): frontier goals no longer land against walls -- raised `goal_clearance` default 0.30 -> 0.6.** User screenshot: a frontier goal (green sphere) sat right against a wall -> Nav2 can't fit the 0.70x0.31 footprint there (goal inside the costmap inflation) -> goal rejected/blacklisted, wasted cycles. Root cause: the code default `goal_clearance` was 0.30, SMALLER than both the footprint reach (0.35m half-length) AND the global-costmap `inflation_radius` (0.7) -> goals fell in the inflation. FIX (frontier_explorer.cpp): default 0.30 -> **0.6** (~ inflation_radius). `findFrontiers`' `hasClearance` already omits any frontier CLUSTER whose nearest >=min_goal_distance cell has a wall within goal_clearance -> with 0.6, near-wall frontiers are OMITTED and the explorer looks for a freer point/cluster (exactly the requested behavior); `clampGoal`/`cellNavigable` use the same threshold so stepping points are also >=0.6m clear. VERIFIED: build clean. Maze OK (2.4m doorways / 6x4 rooms -> ample >=0.6m-clear frontier space, no over-filtering). TRADE-OFF (documented, real-robot): 0.6 needs ~1.2m-wide gaps; for a real facility with narrow (~0.9m) doorways lower via `-p goal_clearance:=0.45` (still > footprint 0.35 -> reachable) or rooms behind narrow doors won't be targeted. If you also change the costmap `inflation_radius`, keep goal_clearance ~ matched. Additive; pairs with CP45 (short-goal stepping + clear-retry termination). The 0.6 value matches the CP28 validated facility run.

**CHECKPOINT 48 (2026-06-26): MAP-DRIVEN wall-follow inspection (replaces perception FIND_WALL) + YOLOE segmenter + inflation 0.7->0.35; adversarially reviewed (3-lens / 25-agent workflow, 12 findings fixed).** User report: `/inspect_zone` went to the wrong zone, spun (the old `FIND_WALL` 360° scan), came back, picked a wall and followed it incompletely. Asked to REDESIGN: use the zone POLYGON (wall layout we already have) to walk EVERY wall of that exact room, ALWAYS FACING the wall (gauges are wall-mounted), one PANORAMA PER straight wall segment (new panorama on rotating to the next wall), then run the team's verified YOLOE segmentation on each panorama, crop objects, feed the existing image service. Also: inflation_radius too high (planner stuck at corridor mouth). **DELIVERED:** **(1) inflation 0.7->0.35** in BOTH costmaps (`nav2_params_rtab.yaml`) — 0.7 filled the narrow maze corridors edge-to-edge (no clear lane); 0.35 keeps ~0.20m off the footprint edge and still centres the path. **(2) NEW `zone_wall_follower.py`** (left working `zone_sweeper` untouched). Pure module-level `wall_segments(polygon,...)` (unit-testable): `simplify_poly` (merge near-collinear) → per real wall (len≥`min_wall_len`): inward normal via point-in-polygon (winding-agnostic), standoff start/end trimmed off corners, FACE heading looking at the wall; skips walls where standing at standoff would be OUTSIDE the room (concave-safe); keep-LONGEST cap (`max_segments`=12, deterministic, never drops a real wall by start-pose). Per-segment FSM: SEG_NAV (Nav2 to standoff facing wall) → SEG_GATE (`/scan` confirms a wall; doorway openings SKIP; **dead lidar → loud abort**, not silent skip-all) → SEG_SQUARE (`_wall_offset` on a NARROW ±28° cone so it can't lock onto the perpendicular CORNER wall) → SEG_SWEEP (strafe the wall, frames keyed on lateral displacement = frame-independent; front e-stop + lateral `side_clear` gate + no-progress/stall watchdog + TF-loss explicit stop; `_vx_hold` backs off when close, holds vx=0 at mid-wall doorways) → SEG_STITCH (strip panorama `panorama_NN.png` + `frames_NN.json`) → next wall; BACKOFF; writes `panoramas.json` + canonical `panorama.png`. ABORTED-nav validates pose before sweeping; non-zero exit on sensor/localization fault. **(3) NEW `yoloe_segmenter.py`** = user's verified YOLOE+`set_classes` script refactored from a live-webcam loop into a per-panorama batch — **DETECT + CROP ONLY** (user dropped needle/OCR; reading happens downstream on the crops, so `easyocr` dep removed). `imgsz` sized to the panorama (else a 0.15m gauge letterboxes to ~15px and is missed). Writes crops→`gauges/*.png`, `detections.json`, `gauges.json` (with `lateral` order key), contact sheet — exactly where `get_zone_image`/`get_zone_gauges`/`gauge_inspector` already look. **GRACEFUL DEGRADE** (no weights/deps on this laptop, none in Gazebo): writes empty result, exits 0, mission continues. **(4) Wiring:** `inspect_zone()` → wall-follower → yoloe → optional Claude read; old path kept behind `INSPECT_LEGACY=1`. `get_zone_image` now surfaces per-wall `panoramas` + `detections`. setup.py: 2 new entry_points. **ADVERSARIAL REVIEW (Workflow, 3 lenses geometry/integration/safety, 25 agents):** 12 confirmed findings — fixed 2 HIGH (max_segments dropping real walls; square-up locking onto the corner wall), the MEDIUMs (vx dead-zone/doorway-creep/no-front-estop; dead-/scan silent success; unvalidated ABORTED nav), and cheap LOWs (sweep watchdog/stall, TF-loss stop, BUILD timeout, yoloe imgsz, score() KeyError, clean-before-early-return). DEFERRED: `use_sim_time` global threading (pre-existing, whole-stack, real-robot-bringup only — documented). VERIFIED: colcon build clean (x3); `wall_segments` offline-checked on maze (4 walls/room, all standoff inside) + facility (concave rooms now keep only in-room walls); graceful degrade proven (missing weights → empty JSON, rc 0). All additive; mapping/frontier/legacy sweeper untouched.
